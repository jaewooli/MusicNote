# 정확도 개선 작업 지시서

`CLAUDE_CODE_HANDOFF.md`(codex 작성)를 읽고, 실제로 측정한 뒤 정리한 문서다.
핵심 결론: **codex가 만든 평가 하네스는 한 번도 실행된 적이 없고, 그동안의
정확도 판단은 Canon 클립 한 개의 육안 관찰에 근거하고 있었다.**
아래 숫자는 2026-08-30에 `eval/refs`(6클립, 3281음) 로 처음 측정한 값이다.

## 0. 측정된 기준선 (baseline)

### 0-1. 중요한 정정: 두 경로는 서로 다른 엔진이다

처음에 `direct:polyphonic` 으로 잰 값(note F1 0.526)은 **출시 경로가 아니다.**
`eval_harness.py --engine direct:` 는 `transcribe.py` 의 basic-pitch 를 부르는데,
사용자가 `polyphonic` 을 고르면 `app.py:519` 의 `is_mt3` 때문에 **MT3** 가 돈다.
아래 숫자는 실제 MT3 출력으로 다시 잰 것이다.

### 0-2. MT3 경로 (실제 출시 경로)

`python eval/replay_eval.py eval/refs` — MT3 raw 출력 캐시를 재생해서 채점.
6클립 전부 캐시 완료 (2026-08-30).

| 지표 | 값 |
|---|---|
| onset F1 | 0.958 |
| **note F1** | **0.947** |
| note+offset F1 | 0.869 |
| note precision / recall | 0.974 / 0.922 |

클립별: ref00 0.865 · ref01 0.968 · ref02 0.986 · ref03 0.951 ·
ref04 0.934 · ref05 0.979.

게이트 수정 전 같은 경로는 ref00 에서 note F1 **0.175** 였다.

### 0-3. 참고: basic-pitch 경로 (출시 안 됨)

onset F1 0.717 / note F1 0.526 / note+offset F1 0.086,
precision 0.818 vs recall 0.650. 라이브러리 모드용 참고치로만 유지한다.

### 이 숫자가 말하는 것

1. **MT3 자체는 나쁘지 않았다.** 게이트를 끄면 note F1 0.865, note+offset F1
   0.811 이 나온다. 문제는 모델이 아니라 후처리였다.
2. **문제는 과검출이 아니라 미검출이었다.** precision 0.93 / recall 0.81.
   그리고 그 미검출의 대부분은 아래 P1 한 줄에서 나왔다.
3. **basic-pitch 로 관찰한 "음 길이가 2.9배 길다"는 MT3 에 해당하지 않는다.**
   MT3 는 오히려 짧게 낸다(중앙값 30 ms, 정답 45 ms). 원래 P2 로 잡았던
   offset 정제는 출시 경로에서는 우선순위가 크게 낮아진다. 8절 참조.

---

## 1. [P0] 기준선을 회귀 게이트로 고정한다 — 완료

**문제였던 것**: `backend/eval_harness.py` 와 `eval/refs` 가 갖춰져 있는데도
실행 기록이 저장소 어디에도 없었다. 최근 세 커밋은 모두 이 지표에 어떤
영향을 줬는지 확인되지 않은 채 들어갔다.

**한 일**
- `backend/mt3_post.py` (신규): `app._mt3_stems` 에 인라인으로 있던 게이트를
  순수 함수로 분리. env 로 오버라이드 가능하고, **몇 개를 왜 지웠는지
  리포트를 반환**한다. 이전에는 조용히 사라졌다.
- `eval/mt3_cache.py` (신규): 클립당 MT3 raw 출력을 한 번만 캐시한다.
  이 서버에서 MT3 는 실시간의 약 13배라 매번 추론하면 튜닝이 불가능하다.
- `eval/replay_eval.py` (신규): 캐시를 읽어 게이트 + 시퀀스 분리를 재생하고
  채점한다. `--sweep-min-note` 로 파라미터 스윕. **초 단위로 돈다.**
- `eval/test_mt3_gate.py` (신규): 게이트 회귀 테스트.
- `eval_harness.py` docstring 에 direct/api 엔진 차이 경고 추가.

**하네스 자체 검증**: 정답 MIDI 를 그대로 MT3 출력인 척 주입하면
(= 완벽한 채보) note F1 0.992 가 나온다. 채점 배관이 맞다는 뜻이다.

**남은 것**: 정확도에 영향을 주는 변경은 전후 note F1 을 같이 적는다.
올랐는지 내렸는지 모르는 변경은 병합하지 않는다.

---

## 2. [P1] 80 ms 최소 길이 필터 — 완료, 영향이 가장 컸다

**문제**: `backend/app.py:377-382` (수정 전)

```python
amp_thr = int(round(18 * (1.0 - s)))   # s=0.5 -> 9
min_len = 0.05 + 0.06 * (1.0 - s)      # s=0.5 -> 0.08 s
if n["velocity"] < amp_thr or (n["end"] - n["start"]) < min_len:
    continue
```

MT3 는 프레임 피크가 아니라 **이산 노트 이벤트**를 낸다. ref00 에서 MT3 raw
음 길이 중앙값은 30 ms, 88.9 % 가 80 ms 미만이다. 정답도 중앙값 45 ms,
86.4 % 가 80 ms 미만이다 — 즉 짧은 음이 정상이다.
그런데 이 게이트는 **610 음 중 542 음(89 %)을 지우고 있었다.**

결과: note F1 0.175. 완벽한 채보를 넣어도 이 게이트로는 0.564 가 상한이었다.

**추가 발견**: YourMT3 는 **모든 음의 velocity 를 100 으로 고정**해서 낸다.
따라서 velocity 게이트는 한 번도 발동한 적이 없고, 감도 슬라이더는 MT3
경로에서 **오직 짧은 음을 지우는 기능만** 하고 있었다.
`app.py` 의 `conf = 0.55 + 0.4*(velocity/127)` 도 전 음 동일한 상수라
`low_conf` 표시는 의미가 없다.

**한 일**
- 길이 하한을 0.005 s 고정으로 낮췄다(0 ~ 0.008 구간은 점수가 동일).
  길이는 이제 필터가 아니라 **퇴화 이벤트 방어용 검사**다.
- 감도가 길이를 건드리지 못하도록 `MIN_NOTE_SPAN = 0.0` 으로 분리했다.
- 게이트가 버린 개수를 로그로 남긴다.

**결과**: ref00 note F1 **0.175 → 0.865**, note+offset F1 0.152 → 0.811.

**남은 결정**: MT3 경로에서 감도 슬라이더는 이제 아무 효과가 없다.
MT3 가 per-note confidence 를 주지 않기 때문이다. 길이로 대체하면 안 된다
(그게 이 버그였다). 슬라이더를 MT3 모드에서 숨기거나, 별도의 신뢰도
추정을 붙이는 결정이 필요하다. **사용자 판단 필요.**

---

## 3. [P2] 음 길이(offset) 정제 — 우선순위 강등

원래 note+offset F1 0.086 을 근거로 P2 로 올렸으나, 그 값은 basic-pitch
경로의 것이었다. **MT3 경로는 게이트 수정 후 0.811 이다.**

다만 MT3 는 음을 짧게 내는 경향이 있다(20 ms 미만이 35.7 %, 정답은 1.9 %).
mir_eval 의 offset 허용치가 짧은 음에서는 최소 50 ms 로 동작하므로 지표에
잘 안 잡힐 뿐, 악보 표기에서는 문제가 될 수 있다.

**할 일 (낮은 우선순위)**
- 악보용 최소 표기 길이는 채점용 offset 과 분리해서 다룬다.
- `voices.py:91` 의 0.18 s 초과 허용은 연결 판단에만 쓰고 기록되는 `end`
  에는 반영하지 않도록 분리한다.

---

## 4. [P3] 시간 이동 앙상블 — 구현 완료, 측정 대기

**문제였던 것**: `mt3_worker.py` 는 파형 전체를 한 번만 추론했고, codex 는
끝부분 누락에 대해 오른쪽 0.75 s 패딩만 추가했다(`END_PADDING_SECONDS`).
이건 **클립 끝** 문제만 다룬다. 곡 중간 누락은 모델 내부 세그먼트 경계에서
생기므로 패딩으로는 손댈 수 없다.

**한 일**
- YourMT3 설정을 확인했다: `input_frames = 32767` = **2.048 s 비중첩
  세그먼트**. 따라서 이동량은 임의값이 아니라 **반 세그먼트 = 1.024 s** 다.
  이전 경계가 전부 세그먼트 한가운데로 이동한다.
- `mt3_worker.transcribe(..., shift=)`: 앞에 무음을 덧대 추론하고 방출
  시각에서 다시 빼서 원래 타임라인으로 되돌린다. 왕복 검증 완료.
- `backend/mt3_ensemble.py`: 두 실행을 병합하고 음마다 `agreement` 를 붙인다.
  **YourMT3 는 velocity 를 전부 100 으로 내므로 이것이 유일한 per-note
  신뢰도다.**
- **전달과 검토는 별개 결정**이다. 처음엔 한 함수로 묶었는데, 그러면
  union 모드에서 전부 전달되어 검토 큐가 비어 목적이 사라진다.
  전달은 `agreement >= min_agreement`, 검토는 `agreement < 실행 수`.
  각 항목의 `in_score` 로 "악보에 있음 · 확인 필요" 와 "누락 의심 · 승인 시
  추가" 를 구분한다. 자동 반영은 없다.

**측정 결과** (6클립, `--shifts 1024`):

| 모드 | onset F1 | note P | note R | note F1 | +off F1 |
|---|---|---|---|---|---|
| 단일 실행 | 0.958 | 0.974 | 0.922 | 0.947 | 0.869 |
| **union (1)** | 0.964 | 0.956 | **0.951** | **0.953** | 0.874 |
| vote (2) | 0.948 | **0.995** | 0.901 | 0.944 | 0.868 |

**union 을 기본값으로 확정한다.** recall 이 0.922 → 0.951 로 오르고 precision
하락(-0.018)이 그보다 작다. vote 는 precision 0.995 로 거의 오검출이 없지만
recall 을 0.901 까지 떨어뜨려 단일 실행보다도 나쁘다. 다만 "틀린 음이 없는
악보"가 필요한 사용자에게는 vote 가 의미 있는 선택지다
(`MUSICNOTE_MT3_MIN_AGREEMENT=2`).

**비용**: MT3 를 2회 돌리므로 시간이 2배다. 다만 기존에도 매 작업마다
piano-tx 를 돌리고 있었으므로 프로파일은 비슷하다 — 작동하지 않는 쪽에서
작동하는 쪽으로 옮긴 것이다.

### 정정 (2026-09-01): union 은 솔로 피아노에서만 옳았다

위 표는 **`eval/refs` 6클립, 전부 솔로 피아노**로만 측정한 것이다. 밴드 편성
레퍼런스(`eval/refs_band`, 8클립)를 만들고 나서 같은 비교를 다시 하니 결론이
뒤집혔다. 오프셋을 4분의 1 세그먼트 단위로 늘려 8종을 캐시한 뒤 모든 부분집합
× 합의 임계값을 훑었다:

| 설정 | 솔로 P | 솔로 R | 솔로 F1 | 밴드 P | 밴드 R | 밴드 F1 |
|---|---|---|---|---|---|---|
| 단일 실행 | 0.971 | 0.913 | 0.941 | 0.718 | 0.575 | 0.639 |
| 0,1024 union (기존) | 0.949 | 0.944 | 0.946 | 0.598 | 0.676 | 0.635 |
| 0,512,1024,1536 union | 0.919 | 0.951 | 0.935 | 0.463 | 0.738 | 0.569 |
| **0,512,1024,1536 agree>=2** | 0.965 | 0.937 | **0.951** | 0.762 | 0.599 | **0.671** |
| 0,512,1024,1536 agree>=3 | 0.989 | 0.922 | 0.954 | 0.868 | 0.480 | 0.618 |

**밴드에서 union 은 앙상블을 안 돌리는 것보다 나빴다** (0.635 < 0.639). 실행을
추가할 때마다 그 실행의 오검출도 같이 들어오는데 union 에는 그걸 거르는 단계가
없기 때문이다. 솔로 피아노에서 union 이 이긴 건 그 자료에서 모델이 충분히
정확해 "두 번째 의견"이 거의 공짜였기 때문이지, union 이 원리적으로 나아서가
아니었다.

**4실행 + 합의 2 로 바꾼다.** 두 자료 모두에서 개선되는 유일한 설정이다
(솔로 0.946 → 0.951, 밴드 0.635 → 0.671). agree>=3 은 솔로에서 더 좋지만
밴드에서 0.618 로 무너지므로 기본값이 될 수 없다. 밴드 쪽 상위권은
4~6실행 · 합의 약 절반 구간에서 0.667~0.674 로 평평하므로, 균등한 4분의 1
간격은 8클립에 맞춘 값이 아니라 그 평탄부 위의 한 점이다.

**검토 큐**: 밴드에서 전달 음이 4991 → 3472 로 줄지만 그 중 실제 맞는 음의
비율은 59.8% → **76.2%** 로 오른다. 줄어든 만큼은 삭제가 아니라 검토 큐로
가고, 큐에 든 음의 12.1% 가 진짜 누락음이다(기존 2.8%). 다만 큐가 25초당
약 680개까지 커져서 `quality.MAX_REVIEW_CANDIDATES` 로 200개만 표시하고
총계를 따로 내려준다. 정렬 기준은 시간순인데, 길이로 정렬해도 AUC 0.535 로
전혀 순위가 되지 않기 때문이다(6086개 검토음 기준, 상위 100개의 적중률 7.0%
로 기저율 11.6% 보다 오히려 낮다).

**비용**: 4회 실행. GPU 워커에서는 25초 클립이 몇 초, CPU 폴백에서는 기존의
2배다.

---

## 5. [P4] Demucs 와 MT3 가 서로 배타적이다

**문제**: `backend/app.py:524` — `use_demucs = mode == "stems"`.
MT3 경로는 원본 믹스를 그대로 받는다. 혼합 음원에서 스템 분리 후 채보는
표준적인 정확도 향상 수단인데, 지금은 둘 중 하나만 쓴다.
codex 문서에도 이 조합에 대한 언급이 없다.

**할 일**
- 실험 플래그로 `demucs → 스템별 MT3 → 병합` 경로를 만든다.
- 하네스로 `api:mt3` vs `api:mt3+demucs` note F1 을 비교한다.
- 메모리 슬롯(`_STEMS_SLOT`/`_MT3_SLOT`)이 이미 직렬화돼 있으므로
  순차 실행은 가능하다. 대신 소요 시간이 크게 늘어난다.

**완료 기준**: 밴드/보컬 포함 클립에서 note F1 향상이 확인되면
해당 음원 유형에 한해 기본 경로로 승격.

---

## 6. [P5] 누락 검증기 — 완료, 접근 자체를 교체했다

**측정 결과** (`eval/eval_validator.py`):

| clip | 전달된 음 | 실제 누락 | 후보 보고 | 맞은 것 | precision | recall |
|---|---|---|---|---|---|---|
| ref00 | 610 | 132 | 15 | 0 | 0.000 | 0.000 |
| ref01 | 745 | 30 | 5 | 0 | 0.000 | 0.000 |

버그가 아닌지 직접 확인했다. ref00 의 실제 누락은 1.02~24.82 s / MIDI 30~103
에 132 개 퍼져 있는데, 보고된 15 개는 5.24~23.02 s / MIDI 41~74 에 몰려 있고
**하나도 겹치지 않는다.** 후보 중 6 개가 pitch 66 하나에 반복해 찍힌다 —
실제 음이 아니라 공진/배음을 잡고 있다는 신호다.

**구조적 한계**
1. 스캔 범위가 `lo, hi = 40, 84` 라 실제 누락의 **44 % 가 원리적으로 범위
   밖**이다 (ref00 은 48 % 만 범위 안).
2. 2차 확인이 `family="keyboard"` → **piano-tx 전용 모델을 통째로 한 번 더**
   돌린다. piano-tx 자체 note F1 은 0.560 으로 MT3 의 0.947 보다 한참 낮다.
   더 나쁜 모델로 더 나은 모델을 검증하고 있었다.

확인 단계는 별도로 완주하지 않았다. 후보 집합을 **거르는** 단계이므로
precision 0 인 집합에서 유의미한 결과가 나올 수 없고, 비용만 크다.

**한 일**: 후보 출처를 P3 앙상블 불일치로 교체했다. 같은 모델이 같은
오디오를 본 결과라 독립적인 오검출 모드가 없다. CQT 경로는
`MUSICNOTE_CQT_CANDIDATES=1` 로 옵트인 보존해 비교 재현이 가능하다.

**교체 후 측정** (6클립, 정답 대비):

| 지표 | CQT 휴리스틱 | MT3 앙상블 |
|---|---|---|
| 검토 큐 precision | **0.000** | **0.805** |
| 단일 실행이 놓친 음 회수 | 0 / 162 | **99 / 284 (34.9 %)** |

검토 후보 328 개 중 264 개가 실제 정답 음이다. 이제 피아노 롤에 그려도
사용자를 오도하지 않는다 — `TODO.md` 3번 UI 작업의 전제가 충족됐다.

**교훈**: CQT 에너지는 **지속음과 새 어택을 구분하지 못한다.** Canon 클립의
gap 조사에서도 "가장 강한 음"이 대부분 방금 친 음이 계속 울리는 것이었다.
이것이 precision 0 의 근본 이유다.

---

## 7. [P6] 시퀀스 분리 — 버그 3개 수정 완료

### 7-1. 체인 병합 (stage 2) — 완료

`separate_sequences` 가 쌍별 점수를 낸 뒤 재확인 없이 union 했다. 병합은
전이적이라 한 번도 비교되지 않은 그룹이 연결된다: 옥타브씩 떨어진 세 선율이
A-B, B-C 는 통과하고 A-C 는 거부되는데도 셋이 한 시퀀스(28 반음)가 됐다.
모듈이 문서화하고 쌍마다 강제하는 옥타브 규칙을 출력이 위반했다.

첫 수정(모든 교차 쌍 통과 요구)은 `one_off_extension` 을 깨뜨렸다.
`None` 이 **두 가지**를 의미했기 때문이다:

| 상황 | 의미 | 처리 |
|---|---|---|
| 공유 어택 충분 + 음역/간격 실패 | 진짜 충돌 | 거부권 |
| 공유 어택 부족 | 판단 불가 | 기권 |

`_chord_verdict` 가 이제 셋을 구분하고 **실제 충돌만** 병합을 막는다.

### 7-2. 겹침 거부권 (stage 1) — 완료

사용자 제보(13 초 클립, 멜로디가 4 성부 중 3 개로 삼등분)에서 발견.
그 실행의 voice 별 MIDI 를 합치면 `separate_sequences` 의 입력이 그대로
복원되므로 MT3 재추론 없이 재현했다.

MT3 는 이 클립에서 멜로디 음을 다음 어택보다 **0.32 / 0.58 / 1.32 s** 길게
잡았다(어택 간격 0.20 s). 그런데 겹침 0.18 s 초과면 그 contour 를 후보에서
아예 제외했다. 방금 연주한 선율이 자기 연속을 못 잇고 새 contour 가 열리고,
다시 원래 것이 풀리면 되돌아온다 — 단선율 멜로디가 a1↔a2 를 왕복했다.
이 함수가 막겠다고 명시한 바로 그 현상이다.

겹침을 **상한 있는 약한 가중치**로 바꾸고, 선율이 이어질 때 이전 음의 끝을
새 어택 시점으로 자른다(악보의 한 성부는 자기 자신과 겹칠 수 없다).
MT3 의 과도한 지속이 악보로 새는 것도 함께 막힌다.

### 7-3. 옥타브 도약 = 새 성부 (stage 1) — 완료

새 contour 를 여는 비용이 12.0 고정인데 도약 비용은 반음 개수 그대로였다.
즉 **옥타브 도약이 "새 연주자 등장"과 동일 비용**이라 모든 옥타브 점프가
성부를 하나 만들었다. Canon 멜로디는 정확히 그 두 도약 지점에서 잘렸다.
`_leap_cost` 가 옥타브 변위를 음정 클래스가 같은 가까운 이웃으로 다룬다.
진짜 먼 도약(19 반음 이상)은 여전히 새 성부를 연다.

### 7-4. 결과, 그리고 두 증상이 하나였다

Canon 클립 **4 → 3 시퀀스**. 남은 분할은 t=3.24 의 59, t=4.04 의 62 —
사용자가 "들리지 않는다"고 한 그 음들이 세 번째 contour 를 열고 멜로디가
거기로 옮겨간 것이었다. **그 두 음만 빼면 정확히 2 시퀀스**:
54 음 멜로디(0.11~12.54 s 연속) + 14 음 베이스. 유령 음과 쪼개진 멜로디는
별개 문제가 아니라 하나다.

그 두 음이 실제인지는 **추측하지 않는다**. 앙상블로 판정할 일이다.

### 7-5. 남은 것

실제 오디오 시퀀스 회귀가 없다. `eval/refs` 에는 음만 있고 "이 음들이 한
성부"라는 정답 레이블이 없어서 note F1 로는 잡히지 않는다(시퀀스 분리는
음 집합을 보존한다). 사람이 매긴 성부 정답이 필요하다.

---

## 8. [P7] 레퍼런스 세트가 제품과 다르다

**문제**: `eval/refs` 6클립은 전부 MAESTRO 피아노 독주를 fluidsynth 로
렌더한 것이다. `eval/build_refs.py` 주석도 "실제 녹음보다 깨끗해서 F1이
낙관적으로 나온다"고 스스로 밝히고 있다. 그런데 제품의 주 사용처는
YouTube Shorts / 밴드 / 보컬이다. 즉 **지금 숫자는 상대 비교용으로만 유효하다.**

**할 일**
- 실제 녹음 기반 세트를 추가한다 (MAPS/MAESTRO 실연 녹음, Slakh 등).
- 최소한 피아노 독주 / 보컬+반주 / 밴드 3개 카테고리로 나눠 평균을 낸다.
- 카테고리별로 note F1 을 따로 본다. 하나의 평균은 회귀를 감춘다.

**완료 기준**: 소프트폰트 세트와 실녹음 세트의 note F1 차이를 알고 있고,
튜닝이 실녹음 쪽을 기준으로 이뤄진다.

---

## 작업 순서 요약

| 순 | 항목 | 상태 | 효과 |
|----|------|------|------|
| 1 | 기준선 고정 (P0) | **완료** | 이후 모든 판단의 전제 |
| 2 | 80 ms 게이트 (P1) | **완료** | note F1 0.175 → 0.865 (ref00) |
| 3 | 검증기 교체 (P5) | **완료** | precision 0.000 → 앙상블 기반 |
| 4 | 시퀀스 분리 버그 3종 (P6) | **완료** | Canon 4 → 3 시퀀스 |
| 5 | 앙상블 union vs vote | **정정됨** | 솔로에서 union 확정했으나 밴드에서 뒤집힘 → 4실행 agree>=2 (솔로 0.951 / 밴드 0.671) |
| 6 | 감도 슬라이더 처리 | **판단 필요** | MT3 경로에서 무효 |
| 7 | 실제 오디오 성부 회귀 (P6-5) | 대기 | 성부 정답 레이블 필요 |
| 8 | Demucs+MT3 (P4) | 대기 | 혼합 음원 |
| 9 | 실녹음 세트 (P7) | 대기 | 측정 신뢰도 |
| 10 | offset 정제 (P2) | 강등 | MT3 는 이미 0.869 |

## 재현 명령

```bash
# MT3 raw 출력 캐시 (클립당 약 5분, 최초 1회만)
python eval/mt3_cache.py eval/refs
python eval/mt3_cache.py eval/refs --shift 1.024   # 앙상블용 2차 실행

# 후처리 채점 — 초 단위
python eval/replay_eval.py eval/refs
python eval/replay_eval.py eval/refs --sweep-min-note 0.005,0.02,0.08
python eval/replay_eval.py eval/refs --shifts 1024 --min-agreement 1

# 검증기 자체 측정
python eval/eval_validator.py eval/refs --stage candidates

# 회귀 테스트
python eval/test_mt3_gate.py
python eval/test_mt3_ensemble.py
python eval/test_sequence_analysis.py

# 전체 파이프라인 (느림: MT3 는 실시간의 약 13배)
python backend/eval_harness.py --engine api:mt3 eval/refs
```


## 2026-09-01: 합의를 신뢰도로 쓰기, 그리고 실패한 것들

4실행 앙상블로 바꾸고 나니 **음마다 "몇 개의 실행이 이 음을 찾았는가"** 라는
값이 생겼다. 그 전까지 MT3 경로에는 per-note 신뢰도가 아예 없었다 — YourMT3 는
velocity 를 전부 100 으로 내므로 `conf` 가 모든 음에서 0.865 로 고정이었고, UI
의 "불확실한 음" 표시는 장식이었다.

**측정** (두 자료 합쳐 10433 음, 정답 MIDI 대비 onset 50 ms · 동일 음고):

| 합의 | 밴드 맞을 확률 | 밴드 옥타브유령 | 솔로 맞을 확률 | 솔로 옥타브유령 |
|---|---|---|---|---|
| 1 | 27.6% | 13.6% | 24.2% | 22.3% |
| 2 | 41.8% | 10.8% | 42.1% | 26.2% |
| 3 | 51.1% | 6.3% | 89.4% | 5.2% |
| 4 | 88.5% | 2.8% | 99.6% | 0.1% |

"이 음이 맞는가"에 대한 AUC 는 **밴드 0.755, 솔로 0.964**. 앞서 시도한 스펙트럼
기반 옥타브 특징은 0.5(무의미)였다. 옥타브 유령이 낮은 합의에 몰려 있으므로
(13.6% vs 2.8%) 이것이 **유령을 표시할 수 있는 첫 신호**이기도 하다.

**적용한 것 두 가지:**
1. `mt3_post.confidence(agreement, runs)` — 위 표를 풀링한 값을 `conf` 로 낸다.
2. `mt3_post.required_agreement(sensitivity, runs)` — **감도 슬라이더가 드디어
   동작한다.** 이전에는 velocity(상수 100)를 임계값으로 써서 코드 주석이
   "슬라이더는 MT3 경로에서 아무 효과가 없다"고 명시하고 있었다. 실측: 감도
   0.0 → 840음, 0.5 → 1637음, 1.0 → 4260음 (2분짜리 실제 곡).
   이를 위해 `mt3_raw` 에 투표 통과분이 아니라 **병합된 전체**를 저장한다.

### 측정했으나 넣지 않은 것

| 시도 | 결과 | 판단 |
|---|---|---|
| 합의 기반 옥타브 삭제 | 밴드 0.671 → 0.666/0.669/0.671, 솔로 0.951 → 0.948/0.953/0.951 | **불채택.** precision 을 얻고 recall 을 더 잃는다. 옥타브 제거 실패는 이걸로 두 번째(첫 번째는 스펙트럼, F1 변화 +0.000)이므로 옥타브는 **표시만 하고 지우지 않는다** |
| 다이내믹 압축 후 채보 | 압축만 0.637, 원본+압축 8실행 최고 0.664 (기준 0.671) | **불채택.** 조용한 내성을 살리려 했으나 오검출이 더 늘었다. 오디오 전처리 실패는 이걸로 두 번째(첫 번째는 Demucs 스템 분리, −0.129) |
| 병합 onset 허용오차 조정 | 0.03~0.20 전 구간에서 F1 0.666~0.671, 0.05 가 최적 | **현행 유지.** 투표가 버리는 recall 은 매칭 실패가 아니라 실제로 근거가 약한 음이었다 |
| 합의 "패턴"(어느 실행들이 동의했는가) | 인접 실행 44.8% vs 떨어진 실행 38.7% (밴드), 솔로는 42.4% vs 41.8% | **불채택.** 방향도 예상과 반대이고 일관성이 없다. 개수만 쓰는 게 맞다 |
| 교차 모델 앙상블 | 미측정 (이미지가 막고 있음) | 오차가 가장 덜 상관될 축이라 **가장 유망한 다음 실험**. 워커에서 실제로 확인한 차단 원인: `mr_mt3` 는 체크포인트는 있으나 `T5Block.forward() got an unexpected keyword argument 'past_key_values'` — mt3-infer 가 최신 transformers 를 기대하는데 이미지는 yourmt3 때문에 4.44.2 로 고정되어 있다(yourmt3 는 최신에서 사라진 `model_parallel_utils` 를 import 한다). 즉 두 모델은 transformers 버전이 서로 배타적이라 **컨테이너를 나누거나 venv 를 둘 두어야** 한다. `mt3_pytorch` 는 체크포인트를 git-lfs 로 받는데 최소 이미지에 git 이 없다 — `git`/`git-lfs` 를 넣거나 체크포인트를 구워야 한다 |

### 회귀: 악보에서 음이 30% 사라졌던 건

`_absorb_slivers` 의 흡수 기준을 `max(24, 0.20 * 트랙 음수)` 로 두었다. 25초
평가 클립(트랙당 100~500음)에서는 기준이 24~100 이라 무해했지만, 실제 2분 곡의
1086음짜리 피아노 파트에서는 기준이 **217** 이 되어 진짜 성부가 전부 첫 성부로
접혔다. 그 파트는 폴리포니 91%, 동시 최대 7음이라 한 성부에 들어갈 수 없고,
`build_score` 가 안 들어가는 음을 조용히 버려 **1637음 중 490음(30%)이 악보에
찍히지 않았다.** 기존 레이아웃 테스트는 전부 통과했다 — 입력이 작아서였다.

기준을 절대값만으로 되돌렸다. 밴드 라인 57 → 59 로 두 개 늘고 손실은 0%.
`eval/test_score_completeness.py` 가 이 회귀를 직접 잡는다(2분 규모의 4성부
텍스처를 만들어 입력의 98% 이상이 인쇄되는지, 큰 트랙이 흡수 기준을 끌어올리지
않는지 확인).

**교훈**: 평가 클립이 25초라서, 트랙 크기에 비례하는 임계값은 평가에서 절대
드러나지 않는다. 곡 길이에 스케일하는 상수를 새로 넣을 때는 실제 길이의 곡으로
확인해야 한다.


## 2026-09-01 (이어서): 보표를 음이 고르게 하기

실제 2분짜리 피아노 채보의 악보를 감사했더니, 인쇄된 네 성부가 각각 **43·49·51·56
반음**을 오르내리고 있었다. `_plan_staves` 는 성부 하나를 통째로 한 보표에 배정하므로,
낮은음자리표에 놓인 성부가 D6 를 인쇄하는 일이 생긴다. 결과는 **덧줄 6줄 이상이
필요한 음표머리 143개**. 악보를 읽기 어렵게 만드는 건 대부분 덧줄이다.

피아니스트는 그렇게 읽지 않는다. 보표는 **성부가 아니라 음마다** 정해야 한다.
`_split_by_register()` 를 넣어, 두 음역에 걸친 라인을 높은 쪽과 낮은 쪽 두 스트림으로
자른다. 이벤트 단위가 아니라 **음 단위로** 자르므로, 두 음역에 걸친 화음은 같은 tick
에서 위쪽 음은 위 보표에, 아래쪽 음은 아래 보표에 간다 — 실제 피아노 악보 그대로다.

| | 이전 | 이후 |
|---|---|---|
| 덧줄 총합 | 2325 | **748** (이론 최적 713) |
| 덧줄 4줄 이상 음표머리 | 118 | **5** |
| 덧줄 6줄 이상 | 143 | **0** |
| 성부 음역 (피아노) | 43·49·51·56 반음 | 43·29·29·24 반음 |
| 마디 길이 정확도 | 98.3% | 98.6% |
| 채보 → 악보 손실 | 0 | 1음 (동음 겹침이 한 음표머리로 합쳐진 것, 올바른 기보) |
| SVG path 수 | 8072 | 7187 |

렌더링 검증: 47마디 전부 `mismatch=0 oob=0`, 클릭 왕복 오차 0.0000.

자르는 조건은 보수적이다 — 음역 폭 24반음 이상이고 양쪽이 각각 8음 이상일 때만
자른다. 그보다 좁으면 이상치 몇 개 있는 한 음역이고, 자르면 덧줄은 못 줄이면서
쉼표 층만 하나 더 생긴다.

`eval/test_score_completeness.py` 에 회귀 테스트 3개 추가(성부 음역이 좁아지는지,
덧줄이 단일 보표 대비 절반 이하인지, 자르면서 음을 잃지 않는지).


## 2026-09-01 (이어서): 앙상블 매칭 키에서 트랙을 뺐다

`mt3_ensemble._key` 는 실행 간에 같은 음을 `(음고, 트랙)` 으로 짝지었다. 그런데
**MT3 의 프로그램 배정은 세그먼트 경계가 바뀌면 같이 흔들린다** — 네 실행이 모두
찾은 음이 한 실행에서는 track 0, 다른 실행에서는 track 3 으로 온다. 그러면 merge
는 서로 다른 음 네 개로 보고 각각 합의 1 을 주고, 투표가 넷 다 버린다. precision
은 하나도 못 얻고 recall 만 잃는 구조였다.

| 매칭 키 | 밴드 P | 밴드 R | 밴드 F1 | 솔로 F1 |
|---|---|---|---|---|
| `(음고, 트랙)` (기존) | 0.762 | 0.599 | 0.671 | 0.951 |
| **`(음고, 드럼여부)`** | 0.737 | **0.633** | **0.681** | 0.951 |
| `(음고)` | 0.734 | 0.633 | 0.680 | 0.951 |

솔로 피아노는 트랙이 하나뿐이라 키가 달라질 일이 없어 수치가 동일하다. 즉
**밴드에서만 이득이고 솔로에는 무해하다.**

드럼 여부는 키에 남긴다. 드럼 음의 `pitch` 는 음고가 아니라 악기 번호다 — 채널
10 의 38 은 스네어이고, 이걸 진짜 D2 와 합치는 건 무관한 두 사건을 합치는 것이다.
프로그램은 추측이지만 타악/유율 구분은 추측이 아니다.

**부수 효과가 크다.** 실제 곡에서 동시에 울리는 같은 음(유니즌)이 **20.9% → 5.7%**
로 줄었다. 트랙만 다른 중복 음이 이제 하나로 합쳐지기 때문이다. 유니즌은 재생기에서
위상이 겹쳐 +6.0 dB 로 튀던 바로 그 현상이라(같은 날 항목 참조), 채보 정확도와
재생 음량 문제가 같은 원인을 공유하고 있었다.

기존 테스트 `test_different_track_is_not_merged` 는 반대를 주장하고 있었다. 측정에
맞춰 `test_same_note_on_different_tracks_still_agrees` 로 바꾸고, 드럼이 유율 음과
합쳐지지 않는지 확인하는 테스트를 추가했다.

### 남은 악보 손실

실제 곡에서 2012음 중 17음(0.85%)이 악보에 안 찍힌다. 절반은 한 라인 안에서 같은
음이 60 ms 안에 다시 울린 경우, 나머지는 촘촘한 드럼 타격이 양자화 격자에서 같은
칸에 떨어진 경우다. **기보상 한 성부는 같은 tick 에 같은 음을 두 번 찍을 수 없으므로
둘 다 올바른 병합**이지, 조용히 버려지는 버그가 아니다. 더 잘게 쪼개면 없앨 수 있지만
악보가 읽을 수 없게 된다.


## 2026-09-01 (이어서): 4실행 → 3실행

매칭 키에서 트랙을 뺀 뒤 합의 분포가 달라졌으므로, 그 이전 분포에서 고른 앙상블
설정을 다시 골랐다. 0 을 포함하는 모든 오프셋 부분집합 × 모든 합의 임계값을 다시
전수 조사했다. 상위권은 0.680~0.683 으로 평평했고, 그 안에서:

| 설정 | 실행 | 밴드 P | 밴드 R | 밴드 F1 | 솔로 P | 솔로 R | 솔로 F1 |
|---|---|---|---|---|---|---|---|
| 0,512,1024,1536 | 4 | 0.737 | 0.633 | 0.681 | 0.965 | 0.937 | 0.951 |
| **0,1024,1536** | **3** | **0.787** | 0.600 | 0.681 | **0.976** | 0.930 | **0.952** |

F1 은 같고 **두 자료 모두에서 precision 이 더 높으며 추론이 25% 적다.** 동점은
precision 과 비용으로 갈랐다 — 악보에서 틀린 음은 틀린 음으로 보이지만 빠진 음은
음원과 대조해야만 드러나고, CPU 폴백에서는 한 패스가 곡 길이의 약 8배다.

**신뢰도 재보정**(3실행, 8539 음):

| 합의 | 밴드 맞을 확률 / 옥타브유령 | 솔로 맞을 확률 / 옥타브유령 |
|---|---|---|
| 1 | 24.2% / 14.7% | 30.4% / 22.9% |
| 2 | 57.1% / 10.3% | 76.8% / 9.1% |
| 3 | 86.8% / 2.2% | 99.4% / 0.2% |

AUC 는 **밴드 0.811 / 솔로 0.946** 으로 4실행(0.755 / 0.964)보다 밴드에서 낫다.
트랙을 키에서 뺀 덕에 진짜 음 하나가 합의 1 짜리 여럿으로 쪼개지지 않기 때문이다.

배포 임계값(합의 ≥ 2)에서 전달되는 음은 conf 0.61 아니면 0.94 다. **0.5 아래가
없으므로 악보는 깨끗하게 인쇄되고**, 0.7 아래를 테두리로 표시하는 피아노롤에서는
불확실한 음이 그대로 보인다. 악보는 읽는 것, 롤은 확인하는 것 — 의도한 분업이다.

### 이 날의 최종 상태 (실제 2분 곡 기준)

| 항목 | 아침 | 저녁 |
|---|---|---|
| 밴드 note F1 | 0.635 | **0.681** |
| 솔로 note F1 | 0.946 | **0.952** |
| 채보 → 악보 손실 | 30% | **~0%** |
| 덧줄 4줄 이상 음표머리 | 118 | **6** |
| 동시 유니즌 (재생 +6 dB 원인) | 20.9% | **4.7%** |
| 동시 옥타브 | 38.3% | **14.9%** |
| per-note 신뢰도 | 없음 (전 음 0.865 고정) | AUC 0.811 / 0.946 |
| MT3 추론 패스 | 2 | 3 |


## 2026-09-01 (이어서): 파트를 묶는 기준

실제 곡의 악보를 보니 **드럼 보표가 둘, "패드 1" 보표가 둘**이었다. MT3 는 음마다
프로그램을 배정하는데 그 배정이 흔들리므로(같은 날 매칭 키 항목 참조), 한 연주자가
여러 트랙으로 흩어진다.

- **타악기는 전부 한 파트로 합친다.** 채널 10 이벤트의 "프로그램" 은 추측이고
  드럼 키트는 하나다. 둘로 나뉘어 두 번째 보표가 쉼표 54% 였다.
- **유율 악기는 트랙 id 가 아니라 프로그램으로 묶는다.** 트랙 id 는 임의의 슬롯이고
  프로그램이 MT3 가 붙인 악기 정체다. 한 트랙이 여러 프로그램을 담기도 하고 두
  트랙이 같은 프로그램을 담기도 한다.

| 묶는 기준 | 파트 | 라인 | P | R | F1 |
|---|---|---|---|---|---|
| 트랙 id (기존) | 53 | 62 | 0.509 | 0.718 | 0.596 |
| **프로그램** | 54 | 63 | 0.517 | 0.732 | **0.606** |

실제 곡에서 라인 12 → 11, 악보 파트 6 → 5.

### 작은 트랙은 합치지 않는다 (측정 결과)

"음이 두 개뿐인 트럼펫 파트" 같은 게 프로그램 드리프트일 것이라 보고, 작은 트랙을
큰 트랙에 흡수시키는 방안을 재봤다. **아니었다.** 정답 악기 라벨 대비, 1~5음짜리
트랙 중 더 큰 트랙이 이미 담고 있는 악기와 겹치는 건 **33%** 뿐이고, 이는 41음 이상
트랙의 중복률(36%)과 사실상 같다. 크기는 드리프트를 예측하지 못한다. 합쳤다면
셋 중 둘은 진짜 파트를 지우는 것이었다.

**미해결로 남긴 것**: 그래도 47마디짜리 보표에 음표 2개(쉼표 96%)를 위해 한 단을
쓰는 건 악보로서 낭비다. 음을 버리지 않으면서 이걸 줄이려면 축약 총보처럼 여러
작은 파트를 한 보표에 모아야 하는데, 이건 측정으로 결정할 문제가 아니라 표기 방침
결정이라 손대지 않았다.


## 2026-09-01 (이어서): 음 길이 — 앙상블이 버리던 정보

지금까지 잰 건 전부 onset+음고였다. 그런데 악보에 인쇄되는 건 **길이**이고,
"같은 음인데 어떤 건 짧게 어떤 건 길게 채보된다"는 지적도 offset 문제다. 재보니:

| | onset F1 | onset+offset F1 | 맞은 음 중 길이도 맞는 비율 |
|---|---|---|---|
| 밴드 | 0.681 | 0.562 | 82.6% |
| 솔로 | 0.952 | 0.868 | 91.1% |

offset 오차 자체는 중앙값 7~10 ms 로 작지만, **12% 의 음이 실제 길이의 절반 미만**
으로 나온다. 이게 눈에 띄는 실패 모드다.

여기서 놓치고 있던 게 있었다. `merge()` 는 **존재 여부만 투표하고 run 0 의 타이밍을
그대로 쓴다.** 세 실행이 동의한 음에는 끝 지점 추정치가 셋 있는데 둘을 버리고 있었던
것이다. 추가 추론 없이 공짜로 쓸 수 있는 정보다.

| 타이밍 처리 | 밴드 onF1 | 밴드 offF1 | 솔로 onF1 | 솔로 offF1 |
|---|---|---|---|---|
| run 0 그대로 (기존) | 0.681 | 0.562 | 0.952 | 0.868 |
| **end 중앙값** | 0.681 | **0.586** | 0.952 | **0.888** |
| end 최댓값 | 0.681 | 0.583 | 0.952 | 0.875 |

**onset F1 은 그대로이고 offset F1 만 오른다** — 어떤 음이 존재하는지는 건드리지
않으니 당연하다. 최댓값은 밴드에서 비슷하지만 솔로에서 나쁘고 모든 음을 체계적으로
늘려 기보에 겹침을 더 만든다. 중앙값이 두 자료 모두에서 오르는 쪽이다. start 까지
중앙값을 내도 결과는 동일했으므로(밴드 0.573) 이론만 보고 추가하지 않았다.

길이도 맞는 비율: 밴드 82.6% → **86.2%**, 솔로 91.1% → **93.3%**.

### 잘린 음을 다시 잇기 — 불채택

짧게 나온 음이 MT3 가 한 음을 쪼갠 결과일 수 있어, 같은 음고의 연속된 음을 간격
임계값 이하이면 다시 잇는 방안을 쟀다.

| 재결합 간격 | 밴드 onF1 | 밴드 offF1 | 솔로 onF1 | 솔로 offF1 |
|---|---|---|---|---|
| 없음 | **0.681** | **0.586** | 0.952 | 0.888 |
| 0.00 s | 0.654 | 0.548 | 0.959 | 0.895 |
| 0.06 s | 0.618 | 0.505 | 0.956 | 0.888 |

솔로에서는 아주 조금 오르지만 **밴드에서 크게 떨어진다.** 짧은 음들은 쪼개진 게
아니라 MT3 가 실제로 짧게 낸 것이다. 불채택.

### 교차 모델: 한 겹 벗겼지만 여전히 막혀 있다

`mt3_pytorch` 의 첫 번째 차단 원인은 이미지에 git-lfs 가 없어 체크포인트를 못 받는
것이었다. Dockerfile 이 apt 를 피하는 건 **arm64 빌드 호스트에서 qemu 로 gpgv 가
깨지기 때문**이고, x86 워커에서 런타임에 apt 를 쓰는 건 문제없다 —
`MT3_WITH_GIT=1 gpu.sh up` 으로 확인했고 체크포인트는 정상적으로 받아진다.

그런데 그 다음에 `mr_mt3` 와 **같은 벽**이 나온다:
`T5Block.forward() got an unexpected keyword argument 'past_key_values'`.
두 모델 모두 transformers 의 cache 리팩터 이후 버전을 요구하는데, yourmt3 는
4.44.2 를 요구한다(최신에서 사라진 `model_parallel_utils` 를 import). **한
site-packages 로는 공존이 불가능하다.**

컨테이너 안에서 `--target` 으로 새 transformers 를 따로 깔고 두 번째 워커를 8733
포트에 띄우는 방법을 시도했으나, 그 실험용 인스턴스가 이미지 pull 에서 17분이 넘게
걸려(느린 호스트) 시간 안에 끝나지 않았다. 인스턴스는 정리했다.

**제대로 하려면 런타임 꼼수가 아니라 이미지를 두 환경으로 빌드해야 한다.** 오차가
가장 덜 상관될 축이라 여전히 가장 유망한 다음 작업이다.


## 2026-09-02: 측정 도구부터 고쳤다 — `replay_eval` 이 드럼을 유율 음으로 세고 있었다

`eval/replay_eval.py` 의 레퍼런스 쪽(`ref_notes`)은 드럼 트랙을 건너뛰는데, 추정
쪽은 드럼 타격을 그대로 넣고 있었다. 밴드 자료에서 MT3 출력의 **45%가 드럼**이라
precision 이 통째로 깎여 나갔다. 커밋된 하네스가 밴드 F1 을 0.557 로 보고하는 동안
임시 스크립트로 잰 실제 값은 0.688 이었다 — 즉 지금까지의 밴드 수치는 전부 커밋되지
않은 스크립트에서 나온 것이었고, 재현이 안 됐다.

레퍼런스가 유율 음만 담고 있으므로 추정에서도 드럼을 뺀다. 이제 커밋된 명령 하나로
재현된다:

    .venv/bin/python eval/replay_eval.py eval/refs_band --shifts 1024,1536 --min-agreement 2

**현재 기준선** (3실행, 합의 2):

| 자료 | onset F1 | note P | note R | note F1 | +offset F1 |
|---|---|---|---|---|---|
| `eval/refs_band` (8클립) | 0.829 | 0.781 | 0.634 | **0.688** | 0.618 |
| `eval/refs` (6클립, 솔로) | 0.967 | 0.979 | 0.938 | **0.958** | 0.893 |

### 밴드에서 점수가 어디로 새는가

추정 음 3369개 중 94.2%는 onset 이 맞다. 그런데 그중 **24.0%는 음고가 틀렸고, 그
틀린 음의 39.9%가 옥타브 배수**다(+12 가 188개로 최다). 솔로는 같은 값이 각각
2.3%, 11.3%다. 밴드의 옥타브 문제는 솔로의 10배다.

## 2026-09-02: 옥타브 오류의 72%는 "중복"이 아니라 "이동"이다

지금까지 두 번의 옥타브 시도는 모두 **지우는** 방식이었고 둘 다 실패했다. 이유를
이제 알았다. 옥타브 계열 오류 434개를 나눠 보면:

| | 밴드 | 솔로 |
|---|---|---|
| 중복 — 맞는 음도 같이 채보됨 | 121 (28%) | 17 (100%) |
| **이동 — 맞는 음이 아예 없음** | **313 (72%)** | 0 (0%) |

**이동한 음은 지워서 고칠 수 없다.** 지우면 precision 만 얻고 그 음은 영영 잃는다.
고치려면 옮겨야 한다. 그리고 솔로에는 이동이 하나도 없다 — 솔로에서 옥타브를
건드리는 모든 처리는 순손실이다.

### 스펙트럼으로 옮기기 — 불채택 (세 번째 옥타브 시도)

CQT 에서 `log(E(음고-12) / E(음고))` 는 실제로 잘 갈린다. **AUC 0.841**, 맞는 음의
평균 −1.03 대 이동한 음의 평균 +0.49. 그런데 밴드 기준 이동 비율이 7.5%뿐이라
어떤 문턱값에서도 옮기는 쪽이 손해다:

| 문턱 | 옮긴 수 | 밴드 F1 | 솔로 F1 |
|---|---|---|---|
| 끄기 | 0 | **0.688** | **0.958** |
| +0.50 | 332 | 0.683 | 0.926 |
| +1.00 | 149 | 0.693 | 0.944 |
| +1.50 | 34 | 0.686 | 0.952 |

밴드에서 가장 좋은 지점이 +0.005 인데 같은 설정이 솔로에서 −0.014 다. AUC 0.841 은
7.5%의 사전확률을 이기기에 부족하다.

### 그런데 이동은 음 단위가 아니라 **악기 단위**로 일어난다

프로그램별로 갈라 보니 흩어져 있는 게 아니었다:

| 클립 | 프로그램 | 음 수 | +12 비율 |
|---|---|---|---|
| band03 | 33 (핑거 베이스) | 104 | **86.5%** |
| band00 | 80 (리드 신스) | 73 | **83.6%** |
| band00 | 33 | 94 | 39.4% |
| band01 | 80 | 45 | 28.9% |
| band0x | 0 / 2 (피아노) | 1000+ | 0~4% |

**베이스 파트 전체가 한 옥타브 위로 올라간다.** 그래서 세 실행이 전부 같은 실수를
하고, 앙상블 투표는 이걸 볼 수 없다 — 만장일치로 틀린다.

파트 단위로 같은 스펙트럼 증거를 모아 봤지만 갈리지 않았다. 이동률 86.5%인
band03/prog33 의 평균이 −0.05 인데, 이동률 0%인 band03/prog56 은 +0.77 이다.
파트 평균은 그 악기가 어느 음역에서 연주하느냐를 잴 뿐 옥타브가 맞는지를 재지 않는다.

**결론: 옥타브는 이 모델의 출력만 보고는 고칠 수 없다.** 표시만 하고 두는 현재
정책이 맞다. 이건 교차 모델 앙상블이 필요한 이유의 가장 강한 근거이기도 하다 —
서로 다른 학습을 거친 모델은 베이스를 같은 방향으로 틀리지 않는다.

## 2026-09-02: 검토 큐에서 음을 건져 오기 — 불채택

합의 1 짜리 유율 음은 밴드에서 47.3%, 솔로에서 85.0%가 실제로 맞는 음이다(드럼을
빼고 세면 그렇다 — 기존에 기록된 24.2%/30.4%는 드럼이 섞인 수치였다). 그래서
"맞을 만한 것만 골라 살리면" 되지 않을까 싶었다.

쓸 만한 특징도 있었다. CQT 에서 그 음고가 주변 대비 얼마나 두드러지는지
(`prom`)는 AUC 0.693(밴드)/0.800(솔로), 가장 가까운 채택 음과의 음정 거리는
AUC 0.743/0.708 이다.

그런데 **어떤 문턱값에서도 F1 이 단조 감소한다**:

| prom 문턱 | 밴드 P | 밴드 R | 밴드 F1 | 솔로 F1 |
|---|---|---|---|---|
| 끄기 | 0.781 | 0.634 | **0.688** | **0.958** |
| ≥3.0 | 0.747 | 0.650 | 0.682 | 0.956 |
| ≥2.5 | 0.719 | 0.661 | 0.673 | 0.951 |
| ≥2.0 | 0.670 | 0.670 | 0.657 | 0.940 |
| ≥1.0 | 0.573 | 0.711 | 0.630 | 0.925 |

recall 은 오르지만 precision 이 항상 더 빨리 떨어진다. 합의 임계값은 이미 최적점에
있다. 검토 큐는 사람이 보라고 있는 것이지 자동으로 되돌릴 대상이 아니다.

## 2026-09-02: 실행을 더 늘려도 소용없다 — 시프트 앙상블은 포화했다

캐시된 8개 시프트(0/256/512/768/1024/1280/1536/1792) 전부를 써서 다시 쟀다:

| 설정 | 실행 | 밴드 P | 밴드 R | 밴드 F1 | +offF1 |
|---|---|---|---|---|---|
| **0,1024,1536 · 합의 2 (현재)** | **3** | 0.781 | 0.634 | **0.688** | 0.618 |
| 8실행 · 합의 3 | 8 | 0.703 | 0.682 | 0.686 | 0.614 |
| 8실행 · 합의 4 | 8 | 0.764 | 0.646 | 0.689 | 0.620 |
| 8실행 · 합의 5 | 8 | 0.812 | 0.607 | 0.679 | 0.617 |

**추론을 2.7배 하고 F1 은 +0.001 이다.** 시프트는 세그먼트 경계만 흔들 뿐이고,
경계와 무관한 오류(옥타브 이동 같은)는 몇 번을 돌려도 그대로다. 재현율 상한
(8실행 합집합)은 0.789 지만 그 지점의 precision 은 0.504 라 쓸 수 없다.

남은 개선 축은 **다른 모델**이거나 **다른 입력 변환**(음고 이동·시간 신축)이다.
둘 다 새 추론이 필요하다.

## 2026-09-02: 음 길이를 오디오에서 다시 재기 — 불채택

MT3 의 끝 지점 대신 CQT 에서 그 음고의 에너지가 최고치의 일정 비율 아래로 떨어지는
지점을 쓰거나 섞어 봤다. 12가지 변형 전부 크게 나빴다:

| 방식 | 밴드 +offF1 | 솔로 +offF1 |
|---|---|---|
| **모델 그대로** | **0.618** | **0.899** |
| 스펙트럼 (0.5) | 0.246 | 0.437 |
| 절반씩 섞기 (0.5) | 0.429 | 0.778 |
| 둘 중 짧은 쪽 (0.15) | 0.589 | 0.882 |

잔향과 다른 악기의 배음이 같은 CQT 칸을 계속 채우기 때문에 감쇠 지점이 한참
늦게 잡힌다. **MT3 의 길이 추정이 오디오에서 직접 재는 것보다 훨씬 낫다.**

## 2026-09-02: 작은 파트는 못 믿을 파트가 아니다

한 성부가 47마디 동안 음 2개만 갖고 보표 하나를 차지하는 문제를 두고, 작은 파트를
지우거나 큰 파트에 합칠지 정해야 했다. 밴드 레퍼런스에서 파트 크기별 정확도:

| 파트 크기 | 음 수 | 맞은 비율 |
|---|---|---|
| 1–5 | 28 | **89.3%** |
| 6–15 | 119 | 61.3% |
| 16–40 | 147 | 77.6% |
| 41–120 | 1364 | 73.7% |
| 120+ | 1711 | 94.4% |

**작은 파트가 더 부정확하지 않다.** 그리고 음 단위로 봐도 작은 파트의 음 중 큰
파트가 이미 갖고 있는 것은 중앙값 17%뿐이다(0~58%). 즉 대부분은 진짜로 다른 음이다.
지우면 맞는 음을 지우는 것이고, 합치면 없던 음을 큰 파트에 넣는 것이다. **둘 다
안 한다.** 보표가 비어 보이는 건 그 악기가 실제로 그만큼만 연주하기 때문이다.

## 2026-09-02: 악보 감사 도구 — `eval/audit_score.py`

지금까지 악보 품질은 그때그때 임시 스크립트로 쟀다. 커밋된 도구로 만들었다:

    .venv/bin/python eval/audit_score.py uploads/<job>.job.json   # 실제 작업
    .venv/bin/python eval/audit_score.py eval/refs_band/band00.mid  # 캐시된 레퍼런스

MT3 를 다시 돌리지 않고 `/api/score` 와 똑같은 경로로 악보를 다시 만든 뒤,
**독자가 실제로 보게 되는 것**을 보고한다: 채보한 음 중 몇 개가 지면에 올랐는지,
덧줄이 몇 개인지, 각 보표가 얼마나 쉼표인지, 음표 값 분포, 마디 길이가 정확한지,
그리고 **온셋/길이 충실도** — 악보에 찍힌 위치와 길이가 채보된 값과 얼마나 같은지.

타이로 이어진 음은 **머리 하나로 센다.** 그러지 않으면 음이 늘어난 것처럼 보여
"손실 -78개" 같은 값이 나온다.

**길이 충실도는 음표 값이 있을 만큼 긴 음에만 적용한다.** 16분음표보다 짧은 음은
더 짧은 기호가 없어서 늘려 적는 게 맞다. 드럼은 100%가 여기 해당한다(타격은
0.1초 미만이고 16분음표로 적힌다) — 드럼을 섞어 재면 "길이가 15.8배" 같은
무의미한 수치가 나온다.

## 2026-09-02: 보표당 세 번째 성부 — 값을 매겨서 허용

`_merge_two` 는 두 라인을 한 성부로 합칠 때 **다른 라인이 어택하는 지점에서
울리던 음을 자른다.** 성부 상한이 2였으므로, 한 마디를 꽉 채운 온음표가 16분음표로
찍히는 일이 생겼다. 15개 악보(레퍼런스 14 + 실제 작업 1)에서, 음표 값이 있을 만큼
긴 음만 세어:

| 상한 | 값 매김 | 길이 오차 25% 이내 | 심하게 잘림(0.6배 미만) | 인쇄된 성부 수 |
|---|---|---|---|---|
| 2 | — | 68.7% | 8.7% | 112 |
| 3 | 항상 | 71.8% | 5.9% | 123 |
| **3** | **0.15** | **71.7%** | **6.0%** | **117** |
| 3 | 0.22 | 68.7% | 8.7% | 115 |
| 4 | 0.15 | 72.4% | 5.3% | 121 |

**값을 매기면 정확도는 거의 다 얻고 잉크는 절반만 쓴다** — 단순한 보표는 여전히
2성부로 접히고, 접었다가 손해 보는 보표만 3성부를 유지한다. 네 번째 성부는
0.7포인트를 위해 층을 하나 더 그리므로 상한은 3.

부작용: 악보에서 잃는 음이 15개 악보 합계 115 → 116개(1만 음 중)로 사실상 불변,
덧줄 7072 → 7079 로 0.1% 증가.

## 2026-09-02: 마디가 마디만큼 채워지지 않고 있었다

`_emit` 은 **읽을 수 있는 값 하나**를 찍는다. 마지막 남은 공간을 쉼표로 채울 때
`_fill_measures` 는 그걸 한 번만 부르고 `cur = m1` 로 넘어갔다 — 1360틱을 요청하면
2분쉼표(960틱) 하나만 찍히고 **나머지 400틱이 그냥 사라졌다.** VexFlow 는 누적
틱으로 음표를 배치하므로 그 마디는 마디선 앞에서 멈춘다.

고친 것 둘:
- 마디 끝 쉼표는 **이름을 붙일 수 있는 동안 계속** 찍는다.
- `_close_measure`: 그래도 모자라면 마지막 음표를 마디선까지 다시 찍어 본다.
  16분음표 + 잃어버린 16분음표 = 8분음표라면 읽을 수 있고 정확하다.

15개 악보 1539마디:

| | 정확한 마디 | 사라진 틱 합계 | 최악의 마디 |
|---|---|---|---|
| 이전 | 1483 (96.36%) | 2730 | 370 / 1920 (19%) |
| **이후** | **1487 (96.62%)** | **1300** | **40 / 1920 (2%)** |

남은 52마디는 셋잇단 격자와 2분 격자가 한 마디 안에서 섞여 20~40틱이 남는 경우다.
그 길이에는 **셋잇단으로도 일반 값으로도 이름이 없다.** `_pick_subdiv` 가 한 마디
안에서 두 격자를 섞지 않게 해야 없어지고, 여기서 억지로 이름을 붙이면 잘못된 값이
찍혀 좁은 마디보다 나쁘다. 회귀 테스트는 "마디 부족분이 인쇄 최소 단위 미만"으로
건다.

## 2026-09-02: 조표와 음이름 — 임시표를 8.4% 줄였다

세 가지 결함이 겹쳐 있었다.

**1. 단조를 못 읽었다.** `krumhansl_fifths` 는 장조 프로파일만 채점해서, 단조 곡을
나란한조가 아니라 **같은 으뜸음 장조**로 보냈다. band01 은 사단조인데 사장조(+1)로
잡혀 **임시표 70개**를 찍었다 — 제 조표(−2)면 6개다. 장·단조 프로파일을 모두
채점하고 단조는 나란한장조의 조표로 보고하게 고쳤다.

**2. 조표 표가 네 개까지밖에 없었다.** `_FIFTHS_PCS` 가 −4..+5 뿐이라 내림라장조
(−5)나 올림바장조(+6)를 고를 수 없었다. band06 은 −4 로 밀려 24개를 찍었고 −5 면
10개다. −6..+6 전부로 넓혔다.

**3. `spell()` 이 조표를 안 보고 `fifths` 의 부호만 봤다.** 그래서 올림바장조가
제 7음을 E# 이 아니라 F 내추럴로 적고(제 조표를 거스르는 임시표), 다장조는 빌려온
단3도를 D# 로 적었다. 5도권 위치로 다시 썼다:

- **조 안의 음**은 조표의 창(`[fifths-1, fifths+5]`)에 있는 철자를 그대로 쓴다.
  선택의 여지가 없다 — 다른 철자를 쓰면 필요 없는 임시표가 찍힌다.
- **조 밖의 음**은 겹임시표를 배제하고, 같은 거리면 내추럴을 먼저 쓰고
  (라장조의 반음 내린 3도는 E# 이 아니라 F 내추럴이다), 그다음 조표에서
  `CHROMATIC_LEAN` 만큼 올림 쪽으로 치우친 자리를 고른다.

`CHROMATIC_LEAN` 은 15개 악보에서 **실제로 찍히는 임시표 수**로 골랐다:

| lean | +0 | +1 | +2 | +3 | **+4** | +5 | +6 |
|---|---|---|---|---|---|---|---|
| 임시표 | 986 | 961 | 944 | 935 | **861** | 867 | 867 |

+4 는 다장조에서 `C# D# F# G# A#`(Bb 만 내림), 내림마장조에서 `Db Eb F# Ab Bb` 를
낳는다 — 재즈·팝 관행 그대로다.

**합계:**

| | 임시표 | 피할 수 있었던 비율 |
|---|---|---|
| 이전 | 940 | 9.6% |
| **이후** | **861** | **3.8%** |

프런트엔드도 같이 넓혔다. `fifthsToKey` / `KEYSIG` / 조 선택 상자가 −4..+5 만
알고 있어서, **백엔드가 −5 를 고르면 조표 없이(다장조로) 그려지고 모든 음에
임시표가 붙었을 것이다.** ±6 까지 채웠다.

## 2026-09-02: 악보에서 잃던 음의 대부분은 베이스드럼 자리에 앉은 플로어 톰이었다

밴드 레퍼런스로 악보를 만들어 보니 band03 은 채보한 668음 중 **46음(6.89%)이
지면에 없었다.** 전부 드럼이었다.

`_emit` 은 한 선에 한 머리만 찍는다(같은 자리에 두 머리를 겹쳐 찍으면 하나로
보이므로 맞는 동작이다). 그런데 `_DRUM_MAP` 에서 **36(베이스드럼)과 41(로우 플로어
톰)이 둘 다 F4** 였다. 밴드 8클립에서 같은 선에 동시에 떨어진 71쌍 중 **69쌍이
이 조합**이었다(나머지 2쌍은 라이드 두 개 — 그건 실제로 같은 선에 그리는 게 맞다).

41 을 맨 아랫줄 E4 로 옮겼다.

| 클립 | 이전 손실 | 이후 |
|---|---|---|
| band03 | 46 / 668 (6.89%) | **0 (0.00%)** |
| band06 | 27 / 697 (3.87%) | 1 (0.14%) |
| band07 | 21 / 753 (2.79%) | 3 (0.40%) |
| band01 | 8 / 577 (1.39%) | 4 (0.69%) |
| **15개 악보 합계** | **116 / 10310 (1.13%)** | **21 / 10310 (0.20%)** |

회귀 테스트: 이름 붙은 키트 조각들 중 **함께 울릴 수 있는 두 조각이 같은 선을
쓰지 않는지** 검사한다. 같은 그림으로 그리는 게 맞는 쌍(크래시 둘, 라이드 둘,
스네어 둘 …)만 예외로 둔다.

## 2026-09-02: VexFlow 렌더 검사를 커밋했다 — `eval/test_render.py`

성부 수와 마디 길이를 건드렸으므로, 실제로 그려지는지 확인할 것이 필요했다.
`eval/render_check.js` 가 node 에서 `frontend/vendor/vexflow.js` 를 그대로 불러
ScoreDoc 의 모든 마디를 포맷한다 — 앱과 **같은 `Voice.Mode.SOFT`** 로. 부족한
마디를 거부하는 STRICT 로 검사하면 아무도 돌리지 않는 렌더러를 시험하게 된다.

현재 229마디 / 11보표, 한 보표 최대 3성부, 실패 0. 일부러 그릴 수 없는 음표를
넣으면 실패하는 것도 확인했다(무의미한 검사가 아님을 확인).

## 2026-09-02: 고친 하네스로 기존 파라미터를 다시 검증했다

밴드 수치가 전부 드럼에 오염돼 있었으므로, **그 수치로 고른 값들을 다시 골라야
했다.**

**앙상블 합의 임계값** (3실행):

| 합의 | 밴드 P | 밴드 R | 밴드 F1 | 솔로 F1 |
|---|---|---|---|---|
| 1 (합집합) | 0.607 | 0.720 | 0.655 | 0.947 |
| **2 (현재)** | 0.781 | 0.634 | **0.688** | **0.958** |
| 3 (만장일치) | 0.875 | 0.503 | 0.614 | 0.933 |

**최소 음 길이 게이트**:

| min_note | 밴드 F1 | 솔로 F1 |
|---|---|---|
| **0.005 (현재, 아무것도 안 버림)** | **0.688** | **0.958** |
| 0.020 | 0.683 | 0.907 |
| 0.050 | 0.673 | 0.705 |
| 0.080 | 0.611 | 0.520 |

둘 다 **현재 값이 그대로 최적**이었다. 길이 게이트는 아무리 작게 걸어도 손해다 —
MT3 는 짧은 음을 실제로 짧게 내지, 쓰레기를 내지 않는다.

## 2026-09-02: 채택된 음을 쳐내는 것도 손해다 — 양방향 모두 막다른 길

검토 큐에서 건져 오는 실험(위)의 거울상으로, **이미 채택한 음 중 스펙트럼 지지가
약한 것을 버리면** precision 이 올라 F1 이 오를지 쟀다.

| prom 미만 버림 | 밴드 P | 밴드 R | 밴드 F1 | 솔로 F1 |
|---|---|---|---|---|
| 끄기 | 0.781 | 0.634 | **0.688** | **0.958** |
| −1.0 | 0.782 | 0.633 | 0.689 | 0.956 |
| +0.5 | 0.780 | 0.569 | 0.649 | 0.934 |
| +1.0 | 0.781 | 0.500 | 0.599 | 0.898 |

합의 3 은 지키고 합의 2 만 거르는 변형도 최대 0.688 로 본전이다.

**후처리 동작점은 시험할 수 있는 모든 방향에서 국소 최적이다.** 살리는 쪽도,
버리는 쪽도, 옥타브를 옮기는 쪽도, 길이를 다시 재는 쪽도, 실행을 더 늘리는 쪽도
전부 손해거나 본전이다. 남은 건 **다른 모델**과 **다른 입력 변환**(음고 이동 등)
뿐이고 둘 다 새 추론이 필요하다.

## 2026-09-02: 시퀀스 분리는 악보에서 특히 중요하다

`voices.separate_sequences` 를 끄면 어떻게 되는지 처음으로 악보 쪽 지표로 쟀다
(15개 악보):

| | 길이 오차 25% 이내 | 심하게 잘림 | 악보에서 잃은 음 | 성부 | 덧줄 | 정확한 마디 |
|---|---|---|---|---|---|---|
| **분리함** | **71.7%** | **6.0%** | **21 / 10310** | 117 | 7079 | 96.6% |
| 분리 안 함 | 50.8% | 18.4% | **548 / 10310 (5.3%)** | 90 | 6575 | 96.5% |

한 트랙의 모든 음을 한 라인에 몰아넣으면 `_merge_two` 가 서로를 계속 잘라내고,
같은 tick 에 같은 음고가 겹쳐 **26배 많은 음이 사라진다.** 성부 27개와 덧줄 500개는
그 값으로 싸다.

## 2026-09-02: 로컬 CPU 워커가 27초짜리 클립에서 OOM 으로 죽고 있었다

> **이 절의 수치와 원인 설명은 틀렸다.** 최고 사용량은 곡 길이가 아니라 어댑터에
> 박힌 `bsz=8` 한 배치로 정해지고, "7.5 GB → 2.2 GB" 는 요청 중이 아니라 요청
> 뒤의 RSS 였다. 아래 09-04 절을 볼 것. 청크 자체와 트랙 id 수정은 유효하다.

GPU 인스턴스가 반납된 상태에서 로컬 폴백을 쓰려다 발견했다. 커널 로그:

    Out of memory: Killed process (pt_main_thread) anon-rss:7525516kB

YourMT3 는 파일 전체의 2.048초 세그먼트를 **한 번에 묶어** 추론하므로 최고 사용량이
곡 길이에 따라 늘어난다. 11 GB 상자에서 27초 클립이 7.5 GB 를 찍고 죽었다 —
3분짜리 곡은 시도조차 못 한다. 앱 쪽 타임아웃이 2400초라 사용자는 **40분을 기다린
뒤 실패**를 본다.

세그먼트 정수 배(기본 6개 = 12.288초) 단위로 잘라서 넣는다. **세그먼트 경계가
움직이지 않으므로 모델이 보는 창은 완전히 같고**, 최고 사용량만 제한된다. 청크
사이에 `gc.collect()` 를 넣어야 한다 — 백엔드가 예측 배치를 붙들고 있어서 그냥
자르기만 하면 N번째 청크의 최고점이 N−1번째 위에 쌓여 같은 OOM 으로 간다.

실측: 최고 RSS **7.5 GB → 2.2 GB**(모델 로드 시 순간 6 GB), OOM 없음. 정확도는
밴드 4클립(band00~03)에서 통째 추론과 비교해 사실상 같다:

| | onset F1 | note F1 | +offset F1 |
|---|---|---|---|
| 통째로 (캐시) | 0.840 | 0.650 | 0.592 |
| 청크 (6세그먼트) | 0.844 | **0.656** | 0.574 |

세그먼트 창이 같으니 같아야 맞고, 남는 차이는 청크 끝의 무음 패딩 유무에서 온다.

청크 단위로도 트랙 id 가 청크마다 달라지므로, **트랙을 (프로그램, 드럼여부)로
식별**해 청크 사이에서 같은 악기가 같은 id 를 유지하게 했다.

## 2026-09-04: 악보 품질을 MT3 가 아니라 **정답 MIDI** 로 재니 결함이 나왔다

기존 악보 지표(`audit_score.fidelity`)는 "페이지가 **MT3 가 준 것**을 얼마나 그대로
찍었나"였다. MT3 가 틀린 곳에서는 틀린 질문이다. MT3 가 가장 못하는 게 note-off
이고, 악보는 음을 다음 어택까지 늘려 찍는 게 정상이므로, "MT3 와 달라짐"은 개선일
수도 악화일 수도 있는데 옛 지표는 항상 악화로 셌다.

정답 MIDI 기준으로 **페이지와 그 입력을 같은 자로** 재는 지표를 넣었다
(`audit_score.against_truth`). 아래 수치는 전부 캐시된 MT3 실행이 있는 **14클립**
(refs 6 + refs_band 8), 3실행 앙상블 / 합의 2 — 즉 실제 서비스 동작점이다.

### 원인 1: 음의 *끝*이 어택으로 고른 격자에 갇혀 있었다

`_pick_subdiv` 는 한 박의 분할을 **그 박의 어택**만 보고 고른다(어택이 독자가
따라가는 것이고 MT3 가 잘하는 것이므로 옳다). 그런데 음의 **끝**도 같은 격자로
반올림하고 있었다. 어택이 하나뿐인 박은 1분할 격자를 받으므로 **그 박 안의 모든
음 끝이 박 전체로 반올림**된다. 끝에 별도 하한 격자를 준다(`END_SUBDIV_FLOOR`).
끝은 어택 격자 위에 있을 필요가 없다 — `_emit` 은 주어진 구간에 이름을 붙일 뿐이다.

### 원인 2: 쉼표 흡수 규칙이 너무 게걸스러웠다

`_absorb_short_rests` 는 "**앞 음보다 짧은** 간격은 릴리스 잡음"이라는 규칙이었다.
2분음표 뒤라면 **2박짜리 쉼표까지 삼킨다.** MT3 의 길이를 기준으로 골랐기 때문에
공짜처럼 보였던 것이다. **16분음표 상한**으로 바꿨다.

### 두 변경의 값 (정답 대비, 유효 레퍼런스 7클립)

| 설정 | 페이지 +offset F1 | 길이 25% 이내 | 쉼표 비율 | 잃은 음 | 정확한 마디 |
|---|---|---|---|---|---|
| 이전 (끝 격자 1, 옛 흡수) | 0.484 | 60.1% | 0.357 | 19 | 97.20% |
| 끝 4, 옛 흡수 | 0.515 | 66.2% | 0.369 | 19 | 97.20% |
| 끝 8, 옛 흡수 | 0.522 | 67.9% | 0.369 | 22 | 97.11% |
| 끝 4, 16분 상한 | 0.548 | 70.6% | 0.378 | 19 | 97.38% |
| **채택: 끝 8, 16분 상한** | **0.559** | **72.7%** | 0.376 | 22 | 97.29% |
| 끝 8, 흡수 없음 | 0.584 | 78.6% | 0.439 | 21 | 95.75% |

입력이 0.704 이므로 **표기가 잃는 offset F1 이 0.220 → 0.145** 로 줄었다.
흡수를 아예 끄면 0.025 를 더 얻지만 쉼표가 오선마다 6%p 더 찍히고(이 규칙이
막으려던 잡동사니) 정확한 마디가 1.5%p 떨어진다. 끝 격자 8 은 4보다 3음을 더
잃는다(8516 중 19 → 22) — 보수적으로 가려면 4가 대안이다.

성부 병합은 **비용이 0** 이었다(`voices<=2/3` 와 `voices<=12/12` 가 같은 값).

## 2026-09-04: 이 측정 하나를 믿기까지 지표 버그를 셋 더 고쳐야 했다

**(1) 드럼이 길이 지표를 오염시키고 있었다.** `timed_noteheads` 가 파트 구분 없이
평평한 목록을 줘서 **킥드럼(MIDI 36)과 베이스 C2 가 같은 키**로 매칭됐다. 게다가
드럼 히트에는 서스테인이 없어서(MT3 는 몇 ms 로 뱉는다) 32분음표로 찍는 게 **옳은
표기**인데 길이 오차로 계산됐다. 밴드 클립에서 드럼이 전체 매칭의 46.5% 를
차지하며 길이 비율 중앙값을 1.88 까지 끌어올렸다. 파트별 매칭 + 길이는 음정
파트만으로 고쳤다. 드럼은 어택만 본다(50 ms 이내 98.3%).

**(2) 빈 악보가 평균을 끌어올리고 있었다.** `eval/refs_meter` 13클립에는 캐시된
MT3 실행이 **하나도 없다**. `from_caches` 가 조용히 빈 결과를 주고, 빈 악보는
"틀릴 음이 없으므로" 모든 비율 지표가 만점으로 나온다. "21악보 평균"이 실제 8클립
어느 것보다도 높게 나온 이유다. `summarise` 가 이제 명시적으로 건너뛴다.

**(3) 하네스가 실행 1개로 돌고 있었다.** `from_caches(mid, "1024,1536", 2)` —
`shifts` 를 **문자열**로 넘기면 문자 단위로 순회해 `.mt3.s1.json` 같은 없는 경로만
찾는다. 즉 시프트 앙상블 없이 기본 실행 하나로 잰 값이었다. 튜플로 고쳤다.
(순위는 바뀌지 않았지만 크기는 전부 달랐다.)

**그리고 새 지표 자체에도 한계가 있다 — 이건 고칠 수 없어서 범위를 좁혔다.**
`eval/refs` 클립들은 note-off 가 **아티큘레이션이지 표기가 아니다**: 레퍼런스 음
길이 중앙값이 45~73 ms, 77~92% 가 100 ms 미만인 플럭/스타카토 렌더링이다. 그 음악을
제대로 적으면 16분음표(그 템포에서 ~190 ms)이고, mir_eval 은 그걸 전부 오답으로
센다. ref00 은 입력 0.846 → 페이지 0.152 로 나오는데 **페이지가 맞고 레퍼런스에
표기 길이가 없는 것**이다. 밴드에서도 band04(81%)/band06(76%)이 같은 경우다.

그래서 `against_truth` 는 레퍼런스의 100 ms 미만 비율이 60% 를 넘으면
`notational_lengths=False` 로 표시하고, offset 비교는 그렇지 않은 클립(14 중 7)
에서만 집계한다. **onset 과 note F1 은 영향이 없으므로 14클립 전체로 본다:**
입력 note F1 0.804 → 페이지 0.725, onset 0.888 → 0.836.

### 렌더 테스트가 조용히 비어 있었다

`test_render.py` 는 밀집 케이스를 `uploads/*.job.json` 스냅샷에서 가져오는데, 지금
디스크에 남은 스냅샷은 **실패한 잡이라 `mt3_raw` 가 없다.** 그래서 합성 입력
10마디만 검사하며 통과하고 있었다. 밴드 레퍼런스를 항상 쓰도록 바꿨다:
**746마디 / 79오선 / 최대 3성부, 실패 0.**

`eval/test_mt3_worker.py` 도 새로 추가했다 — 워커에는 테스트가 하나도 없었는데,
아래 메모리 가드 두 개는 **동작할 때 눈에 보이는 효과가 없어서** 조용히 깨진다.

## 2026-09-04: **옥타브 이동을 파트 단위로 고쳤다** — 이번 작업 최대 성과

지금까지 기록한 것: 옥타브 오류는 **악기 단위로 체계적**이라(band03 베이스 104음
중 86.5%가 통째로 한 옥타브 위) 모든 시프트 실행이 같은 실수를 하고 투표로는 보이지
않는다. 그리고 **매칭된 음의 28%** 가 옥타브 오류다 — 이 파이프라인의 다른 모든
것을 합친 것보다 크다.

먼저 **천장**을 쟀다. 파트마다 정답과 가장 잘 맞는 옥타브를 고르는 오라클:

| | band00 | band01 | band03 | band06 | 평균(8클립) |
|---|---|---|---|---|---|
| 현재 | 0.688 | 0.547 | 0.682 | 0.455 | 0.688 |
| 파트별 오라클 | **0.783** | 0.580 | **0.890** | 0.466 | **0.732** |

band03 은 **+0.208**, band00 은 **+0.095**. 만들 가치가 있다.

### 왜 전에 실패했는가

이전 시도는 **CQT 살리언스**로 음 하나하나를 판정했다. 그건 원리적으로 안 된다 —
진짜 음이 p−12 에 있으면 **p 에도 에너지가 있다**(2배음). "p 에 에너지가 있나?"는
어느 쪽이든 예라고 답한다. 특징 AUC 0.841 이 나오고도 적용하면 F1 이 떨어진 이유다.

`_harmonic_nmf` 는 피치마다 **고정 배음 빗**을 맞춘다. p 의 템플릿은 p, 2p, 3p…
를 설명하고 p−12 의 템플릿은 p−12, p, p+7, p+12… 를 설명한다. 옥타브 문제는 이
분해가 답하도록 만들어진 바로 그 질문이다. 그리고 판정을 **음 단위가 아니라 파트
단위로** 한 번만 한다 — 이동이 악기 단위로 체계적이니 증거를 파트 전체에서 모은다.

### 그래도 그냥 argmax 는 안 된다

| 밴드 8클립 | note F1 | 이동 | 그중 오답 |
|---|---|---|---|
| 보정 없음 | 0.688 | – | – |
| **단순 argmax** | **0.613** | 14 | 9 |
| 가드 적용 | **0.725** | 2 | **0** |
| 오라클 | 0.732 | – | – |

단순 argmax 는 **아무것도 안 하느니만 못하다.** 한 옥타브 아래 빗이 위쪽 빗이
설명하는 것을 전부 설명하고 **덤까지** 설명하므로 무승부를 항상 이긴다. 그런데
마진이 갈린다 — 맞는 이동은 3.9~7.3배, 틀린 이동은 1.1~1.9배였다. 가드 두 개:

- `OCTAVE_RATIO` — "그대로 두기"를 이 배수만큼 이겨야 움직인다
- `OCTAVE_MIN_NOTES` — 파트에 이만큼 음이 있어야 평균에 의미가 있다
  (band03 에서 틀리게 움직인 두 파트는 음이 12개와 5개였다)

**비율 2.0~3.5 × 최소음수 20~60 의 12개 셀이 전부 0.725 / 이동 2 / 오답 0** 으로
같다. 점이 아니라 **평지**다. 가운데인 2.5 / 40 을 쓴다.

### 결과

| | 이전 | 이후 |
|---|---|---|
| 밴드 note F1 | 0.688 | **0.725** (오라클의 84%) |
| 솔로 note F1 | 0.958 | **0.958** (이동 0회) |
| 페이지 note F1 (정답 대비) | 0.725 | **0.745** |
| 페이지 +offset F1 | 0.559 | **0.583** |

**추론 비용이 0이다** — 이미 계산하는 CQT 위에서 도는 후처리다. 솔로 세트에서는
**한 번도 발동하지 않는다**: 솔로 피아노에는 체계적 이동이 없고 가드가 그걸 옳게
알아본다. 전조 실행(+0.014)보다 2.6배 크고 공짜다.

## 2026-09-04: 타임스트레치는 부정적이다 — 온셋은 버티고 **음정이 무너진다**

YourMT3 어댑터에는 이미 타임스트레치 모드가 있다(`adaptive` / `stretch_factors`).
시프트 앙상블은 세그먼트 경계를 시간축에서 옮길 뿐 한 세그먼트 안의 음악은 그대로
두는데, 타임스트레치는 **세그먼트당 음 개수**를 바꾸므로 진짜 다른 축이다. 다만
라이브러리의 선택 기준이 "음이 제일 많은 실행 승리"라 오탐을 보상하므로, 원시
실행만 받아 기존 union/agreement 병합에 먹이려 했다.

먼저 스트레치 실행을 **단독으로** 채점했다(librosa 위상 보코더, 0.8배):

| 클립 | onset F1 (기본 → 0.8×) | note F1 (기본 → 0.8×) |
|---|---|---|
| band00 | 0.821 → 0.703 | **0.600 → 0.363** |
| band01 | 0.773 → 0.608 | **0.513 → 0.393** |

**온셋은 대체로 버티는데 음정이 무너진다.** 위상 보코더가 배음 구조를 뭉개는
전형적인 신호이고, 음정 디코더가 읽는 게 바로 그 구조다. 이 정도로 나쁜 실행은
합의 2 앙상블에 표가 아니라 잡음만 보탠다. 더 나은 스트레처(Rubber Band)는 이
상자에 설치돼 있지 않다.

### 그래서 대신 **리샘플링**을 본다

위상 보코더는 템포만 바꾸려다 스펙트럼을 망친다. 리샘플링은 음정과 템포를 **같은
인수로 함께** 옮기므로 뭉갬이 없고, 변환이 정확히 가역적이다 — 반음 수를 빼고
시간을 곱하면 원래 좌표로 돌아온다. 모델에게는 다른 음역·다른 세그먼트 경계라는
다른 질문이면서 스펙트럼은 깨끗하다.

리샘플 실행은 **기본 실행과 대등하다** — 스트레치처럼 무너지지 않는다. 밴드
8클립 단독 note F1(기본 → 반음↑ 리샘플):

| band00 | band01 | band02 | band03 | band04 | band05 | band06 | band07 | 평균 |
|---|---|---|---|---|---|---|---|---|
| .600→.594 | .513→.481 | .801→**.844** | .684→.663 | .888→**.920** | .507→.451 | .402→**.519** | .793→.785 | .649→**.657** |

**전조 실행이 기본 실행보다 평균적으로 오히려 낫다.** band06 은 0.402 → 0.519
(상대 +29%).

앙상블에 4번째로 넣으면 이득이 있는데, 4번째 실행을 더하는 것 자체가 투표 기준을
낮추므로(2/4 는 2/3 보다 무른 문턱) 캐시된 **다른 시프트**를 4번째로 넣는 대조군이
반드시 필요하다. 밴드 8클립 전체:

| 3시프트에 더한 4번째 실행 | onset F1 | note F1 | +offset F1 | 음 수 |
|---|---|---|---|---|
| 없음 (현재) | 0.829 | 0.688 | 0.618 | 421 |
| 시프트 512 하나 더 | 0.841 | 0.692 | 0.620 | 472 |
| 시프트 768 하나 더 | 0.846 | 0.688 | 0.614 | 481 |
| **반음↑ 리샘플** | **0.846** | **0.702** | **0.628** | 472 |

시프트를 하나 더 쓰면 note F1 이 +0.004 / +0.000 이다 — 사실상 없다. 앞서 "실행
8개 ≈ 실행 3개"로 시프트 앙상블이 포화라고 기록한 것과 일치한다. 같은 추론 한 번을
**전조**에 쓰면 **+0.014**(같은 음 수 472에서). 시프트 실행들은 서로 강하게
상관돼 있고(같은 음역·같은 스펙트럼, 창만 이동) 전조 실행은 실제로 다른 것을 본다.

**이번 작업에서 채보 정확도를 움직인 유일한 결과다.** 비용은 추론 패스 1회
(3회 → 4회, +33%). 밴드 세트에서만 측정했다.

구현은 넣되 **기본은 꺼 둔다** — 네 번째 모델 패스는 비용과 지연이 실제로 늘고,
그 판단은 켜는 쪽이 해야 한다. `MUSICNOTE_MT3_ENSEMBLE_TRANSPOSE=1` 로 켠다.
`mt3_bridge.transcribe(semitones=k)` 가 리샘플 사본을 만들고 결과를 되돌린다.
되돌릴 때 **드럼은 건드리지 않는다** — GM 드럼 번호는 음정이 아니라 키트 자리라,
리샘플한 킥도 킥으로 돌아오므로 반음을 빼면 다른 악기로 바뀐다.

### 그런데 예측한 메커니즘은 **틀렸다**

옥타브 오류를 깰 거라고 예상했는데 아니다. 앙상블 음을 정답과 대조해 분류하면:

| | 정확 | 옥타브 오류 | 그 외 오답 | 놓친 정답 |
|---|---|---|---|---|
| band00 3시프트 | 313 | 119 | 34 | 73 |
| band00 +리샘플 | 325 | **123** | 75 | 57 |
| band01 3시프트 | 156 | 98 | 44 | 114 |
| band01 +리샘플 | 159 | **113** | 71 | 96 |
| band02 3시프트 | 211 | 6 | 10 | 60 |
| band02 +리샘플 | 221 | **5** | 14 | 51 |

**옥타브 오류는 줄지 않는다(오히려 늘어난다).** 이득은 전부 재현율에서 온다 —
놓친 정답 247 → 204. 8클립 전체에서 같은 패턴이다(band05 32→51 / 놓침 489→442,
band06 73→100 / 493→426, band07 20→39 / 102→65). **다른 음역에서 들려줘도 모델은
같은 옥타브 실수를 반복한다.**

그리고 이 표가 **남은 채보 문제의 크기**를 처음으로 정확히 보여준다: band00 은
매칭된 432음 중 **119음(28%)이 옥타브 오류**다. 이번 세션에 내가 움직인 그 어떤
것보다 크고, 시프트 앙상블 · 스펙트럼 교정 · 합의 임계값 · 전조에 **전부 면역**
이다. 남은 헤드룸이 같은 모델의 다른 시점이 아니라 **두 번째 모델**을 요구한다는
가장 강한 증거다.

## 2026-09-04: 옥타브 보정 뒤 오류 예산을 다시 쟀다 — 이제 **재현율**이 문제다

고칠 것을 짐작하지 말고 매번 다시 재기로 했으므로, 옥타브 보정 후 밴드 8클립의
추정 음 3369개를 정답과 대조해 분류했다:

| | 개수 | 추정 음 대비 |
|---|---|---|
| 정확 | 2700 | 80.1% |
| **정답에 있는데 못 찾음** | **1374** | – |
| 아직 옥타브 오류 | 318 | 9.4% |
| 시간 맞고 음정 틀림 | 204 | 6.1% |
| 그 자리에 정답 음이 없음(오탐) | 120 | 3.6% |
| 반음~온음 어긋남 | 27 | 0.8% |

**순수 오탐은 3.6%뿐이다.** 정밀도는 이제 문제가 아니고, 정답 4600여 음 중
**1374개를 아예 못 찾는 재현율**이 남은 병목이다.

그래서 합의 임계값을 다시 골랐다 — 후보 집합이 깨끗해졌으니 투표의 값이 달라졌을
수 있다. 결론은 **그대로 2**다(밴드 a1 0.680 / a2 **0.725** / a3 0.649, 솔로
a1 0.947 / a2 **0.958** / a3 0.933). 투표를 풀어서 얻을 재현율은 없다.

재현율을 올리는 건 앞서 찾은 **전조 실행**이고, 옥타브 보정과 **겹치지 않고 더해진다**
— 보정은 이미 찾은 음을 고치고(정밀도), 전조는 못 찾던 음을 찾는다(재현율):

| 옥타브 보정 | 전조 | P | R | F1 |
|---|---|---|---|---|
| – | – | 0.781 | 0.634 | 0.688 |
| – | ✓ | 0.749 | 0.676 | 0.702 |
| ✓ | – | 0.817 | 0.671 | 0.725 |
| **✓** | **✓** | 0.779 | **0.709** | **0.734** |

재현율 0.634 → 0.709. 그래서 전조를 **패스가 싼 곳에서는 켜기로** 했다: GPU 는 한
패스가 몇 초지만 CPU 폴백은 곡 길이의 8배라 4번째 패스가 40분 대기를 53분으로
만든다. 정확도가 그만한 값은 아니다. 환경변수로 양쪽 다 강제할 수 있다.

## 2026-09-04: **정확도가 왜 안 올라가는가** — 못 찾은 음 1649개의 출처를 전부 추적했다

밴드 8클립, 정답 4419음. 놓친 음 하나하나가 **어디서** 사라지는지 일대일 매칭으로
분류했다. (첫 시도는 틀렸다 — 옥타브 겹침이 있는 음악에서 "한 옥타브 옆에 있나?"는
항상 예라서, band04 피아노가 502음 중 502개 정확 **그리고** 523개 옥타브 오류로
나왔다. 100%를 넘는 순간 매칭이 일대일이 아니라는 게 드러났다.)

| | 개수 | 정답 대비 | 놓친 것 대비 |
|---|---|---|---|
| 정확히 전달됨 | 2770 | 62.7% | – |
| **모델이 그 음높이로 낸 적이 없음** | 634 | 14.3% | **38.4%** |
| **실행에는 있었는데 투표/게이트가 버림** | 577 | 13.1% | **35.0%** |
| 진짜 옥타브 오류 | 238 | 5.4% | 14.4% |
| 옥타브 옆으로만 나옴 | 200 | 4.5% | 12.1% |
| (게이트가 버린 것) | 11 | – | 0.8% |

### 634개는 모델의 한계다 — 후처리로 못 건드린다

세 실행 중 어느 것도 그 시각 그 음높이를 낸 적이 없다. 앙상블·투표·보정 무엇도
없는 것을 만들 수 없다.

### 577개는 있는데 못 건진다 — 이유를 이번에 정확히 알아냈다

투표가 보류한 음들이다. 전부 받으면 F1 이 **떨어진다**(union 0.680 대 0.725).
보류 풀 1759개 중 **진짜는 35.9%뿐**이기 때문이다.

그래서 "순위를 매겨 좋은 것만 건지자"를 제대로 시도했다. 이전엔 **음 길이**로
줄 세웠고 AUC 0.535 — 순위가 아니었다. 이번엔 **배음 지지도**(`_harmonic_nmf` 가
그 음높이·그 구간에 실제로 얼마나 에너지를 두는가)로:

    AUC 0.741  (길이 0.589 대비)
    상위 5%: 78 진짜 / 10 가짜 (정밀도 89%)
    상위 10%: 129 진짜 / 47 가짜 (정밀도 73%)

**순위는 진짜로 작동한다. 그런데 F1 은 안 움직인다:**

| 밴드 | P | R | F1 | 건진 수 |
|---|---|---|---|---|
| 안 건짐 | 0.817 | 0.671 | **0.725** | 0 |
| 보수적 | 0.809 | 0.679 | 0.726 | 88 |
| 공격적 | 0.721 | 0.723 | 0.715 | 880 |

솔로 세트는 모든 문턱에서 더 나빠진다. **이유가 산수에 있다:** 이미 P=0.817 인
집합에, 기저율 35.9% 인 풀에서, AUC 0.741 짜리 순위로 잘라낼 수 있는 고정밀
구간은 **88개뿐**이고 그건 +0.001 이다. 더 파고들면 정밀도가 기존 집합보다
낮아진다. 지금까지 모든 구제 시도가 실패한 이유가 이거다 — 이제 "실패했다"가
아니라 **왜 실패하는지**를 안다.

### 옥타브는 사실상 다 짜냈다

파트별 오라클 0.732, 출하된 검출기 0.725 — **파트 단위 여지는 0.007** 남았다.
남은 것은 파트 안에서 음마다 다른 경우인데, 음 단위로는 **기저율이 막는다**:

    받아들여진 음 2330개 중 실제로 한 옥타브 높은 것은 68개 (3%)
    support(p-12)/support(p) 의 AUC = 0.810  (순위 자체는 좋다)
    어떤 문턱에서도 정밀도 5~18% — 옮기면 맞는 음을 더 많이 망친다

AUC 0.810 이어도 3% 기저율에서는 쓸 수 있는 문턱이 없다. 이건 검출기를 더 잘
만들어서 해결되는 문제가 아니다.

### 결론: 남은 27%는 **두 번째 모델**을 요구한다

놓친 것의 38%는 모델이 낸 적 없고, 35%는 있지만 진짜와 가짜를 가를 신호가 없다.
시프트 실행은 **같은 모델의 같은 의견**이라 둘 다에 무력하다 — 없는 것을 만들지도,
독립적인 확신을 주지도 못한다. 독립적인 두 번째 모델은 **양쪽 다** 건드린다:
낸 적 없는 음을 낼 수 있고, 그 동의는 시프트 실행의 동의와 달리 진짜 증거다.

## 2026-09-04: **근본 원인을 찾았다 — 모델이 저음역을 못 듣는다**

"모델이 낸 적 없음 634개"를 모델의 한계로 적어두고 넘어갈 뻔했는데, 그 634개가
**무엇인지** 봤다. 두 가지가 나왔다.

**절반은 사실 타이밍 문제였다.** 50 ms 창으로 분류했으므로, 모델이 냈지만 80 ms
늦게 놓은 음은 "낸 적 없음"으로 셌다. 창을 넓히니 991개 중 491개(49.5%)가
**있긴 있는데 50~500 ms 어긋나 있었다.**

**그리고 놓친 음은 체계적으로 낮다.** 놓친 음의 중앙 음높이 45(A2), 찾은 음은
63(D#4). 길이와 동시발음 수는 **양쪽이 똑같다** — 밀집도나 짧은 음 문제가 아니라
순수하게 음역 문제다.

음역별로 재현율을 재니 명확하다:

| 음역 | MIDI | 정답 음 | **재현율** | 정밀도 |
|---|---|---|---|---|
| C1–B1 | 24–35 | 296 | **6.1%** | 57.1% |
| C2–B2 | 36–47 | 1069 | 66.3% | 77.4% |
| C3–B3 | 48–59 | 680 | 64.9% | 89.6% |
| **C4–B4** | 60–71 | 1485 | **89.6%** | 96.2% |
| C5–B5 | 72–83 | 865 | 65.7% | 88.6% |
| C6–B6 | 84–95 | 24 | 83.3% | 37.5% |

**MIDI 36 아래에서는 음의 6%밖에 못 찾는다.** 모델은 가운데 음역에 강하게
쏠려 있고, 위아래로 급격히 무너진다.

그리고 이 하나가 **모든 실패 유형**을 설명한다:

| 유형 | 개수 | 중앙 음높이 | MIDI 48 미만 비율 |
|---|---|---|---|
| 제때 찾음(≤50 ms) | 3428 | 62.0 | 25.1% |
| 50~500 ms 어긋남 | 491 | 49.0 | 40.7% |
| **아예 못 냄** | 76 | **32.0** | **93.4%** |
| 멀리 떨어져 나옴 | 424 | 39.5 | 55.4% |

낮을수록 잃는다. **옥타브 이동도 같은 원인이다** — MIDI 33 의 베이스는 거의 안
들리니 모델은 2배음이 들리는 한 옥타브 위에 적는다. band03 베이스가 통째로 한
옥타브 위였던 것, 저음역 재현율이 6%인 것, 못 찾은 음이 저음에 몰린 것이 전부
**같은 하나의 원인**이다.

### 그래서 검증 가능한 예측이 나온다

오디오를 **위로 리샘플**하면 베이스가 모델의 home 음역으로 들어간다(24–35 → 36–47,
재현율 6% → 66% 구간). 변환은 정확히 가역적이므로 되돌리면 된다. 그리고 4번째
투표 실행으로 넣는 것만으로는 **안 된다** — 전조 실행만 들을 수 있는 베이스 음은
표가 1개라 합의 2에서 보류된다. 다른 실행들이 그 음역에서 **측정된 대로 귀머거리**
라면 불일치는 반대 증거가 아니다. 그래서 컷오프 아래에서는 전조 실행을 단독으로
믿는다. 위쪽은 그대로 둔다 — 거기는 기본 실행이 좋고 전조 실행은 오히려 자기
home 음역 밖으로 밀려나 있다.

## 2026-09-04: 마디선 위상은 고치지 못했다 — 세 가지 신호를 재보고 접었다

`_structure` 는 마디 위상을 액센트 대비로만 고르고, 13클립 중 5개에서 틀린다
(mir_eval 로 각 후보 위상을 직접 채점해 확인). 맞히면 그 클립들은 downbeat F1 이
1.00 이 되므로 0.592 → 0.97 이 걸려 있다. 세 신호를 만들어 쟀다:

| 신호 | 맞힌 클립 | downbeat F1 |
|---|---|---|
| 액센트 (현재) | 8/13 | 0.592 |
| **화성 변화**(비트별 크로마 novelty + 베이스 변화) | 9/13 | 0.669 |
| 시작 위치 | 6/13 | 0.457 |
| 마디 자기유사도 | 7/13 | 0.510 |
| 액센트 + 자기유사도 (가중 0.25~0.5) | 9/13 | 0.664 |

**시작 위치 신호는 구조적으로 무의미했다** — 비트 격자가 첫 음에서 시작하도록
만들어져 있어 항상 위상 0을 고른다. 신호가 아니라 항등식이다.

화성 변화는 액센트와 **같은 클립에서 같은 답으로 틀린다**(mref00/03/06 은 둘 다
확신을 갖고 틀린 위상을 고른다). 둘 다 "여기서 무슨 일이 일어나는가"를 묻기
때문이다. 자기유사도는 **다른 곳에서 틀려서** 액센트의 5개 실패 중 4개를 맞히고,
둘의 합집합은 12/13 이다 — 그런데 가중합으로는 9/13 밖에 못 뽑아낸다. 신뢰도
스케일이 서로 맞지 않는다.

**+1클립(13개 중)은 잡음이고 가중치 평지도 0.25~0.5 로 좁다. 그래서 넣지 않았다.**
과적합된 변경을 넣느니 측정과 이유를 남긴다. 이건 여전히 남은 표기 정확도 항목 중
가장 크다 — 마디선이 밀리면 독자에게는 곡 전체가 틀린 것으로 보인다.

`eval/eval_meter.py` 의 기본 `--source audio` 는 **은퇴한 코드 경로**를 재고 있었다.
그 분기는 `downbeats: []`, `time_sig: (4,4)` 를 하드코딩하고 "지금 파이프라인은
둘 다 검출하지 않는다"는 주석을 달고 있는데, 이제 `meter.detect` 가 둘 다 한다.
그래서 기본 실행이 제품을 실제보다 훨씬 나쁘게 보고하고 있었다:

| | 박자표 정확 | downbeat F1 | beat F1 |
|---|---|---|---|
| `--source audio` (은퇴한 경로) | 6/13 | 0.000 | 0.818 |
| `--source midi` (실제 검출기) | **10/13** | **0.592** | 0.868 |

기본값을 `midi` 로 바꿨다.

그런데 실제 경로에서도 **13클립 중 4개가 downbeat F1 정확히 0.000** 이다 — beat F1
은 0.98~1.00 인데도. 박은 맞는데 **마디의 위상이 통째로 1~2박 밀려 있다**는 뜻이고,
악보에서는 모든 마디선이 잘못된 자리에 서 있다는 뜻이다.

`_structure` 는 마디 위상을 "다운비트 액센트 평균 − 나머지 박 액센트 평균"으로
고른다. 각 후보 위상의 점수를 정답과 대조해 보면:

| 클립 | 고른 위상 | 점수 | 정답 위상 | 점수 | 정답 순위 |
|---|---|---|---|---|---|
| mref00 (4/4) | 2 | +0.667 | 0 | +0.464 | 2/4 |
| mref03 (4/4) | 1 | +0.201 | 0 | −0.069 | 3/4 |
| mref06 (3/4) | 1 | **+1.352** | 0 | **−0.489** | 2/3 |
| mref07 (3/4) | 2 | **+1.101** | 1 | +0.071 | 2/3 |

mref06/07 은 **차이가 크게 벌어져 있다** — 액센트 신호가 아깝게 진 게 아니라
자신 있게 틀렸다. prior 를 만지는 정도로는 뒤집히지 않는다는 뜻이고, 화성 변화나
베이스 음 변화 같은 **다른 신호**가 필요하다. 13클립에 맞춰 상수를 돌리면
과적합이므로 여기서 멈추고 기록만 남긴다. **남은 표기 정확도 항목 중 가장 값이
큰 것이다** — 마디선이 밀리면 독자에게는 곡 전체가 틀린 것으로 보인다.

## 2026-09-04: 청크로 OOM 을 고쳤다는 앞 절의 주장은 틀렸다

앞 절(09-02)의 "**최고 RSS 7.5 GB → 2.2 GB**"는 잘못된 측정이다. 그 2.2 GB 는
요청이 **끝난 뒤의** RSS 였고 요청 **중의** 최고점이 아니었다. 실제로 확인한 것:

- 청크 코드가 도는 워커(파일 수정 10:39:50, 워커 기동 10:48:05)에서 band00 한
  클립의 최고 RSS 는 **6.3 GB** 였다.
- 같은 배치 실행에서 **band04~07 이 전부 실패**했고 커널 로그에 OOM 이 또 있다
  (`anon-rss:7908500kB`, 10:47:57). band04 는 28.2초로 **성공한 band01(30.0초)
  보다 짧다** — 길이 문제가 아니었다.

원인은 두 가지다.

**(1) `bsz` 가 어댑터에 8로 박혀 있다.** `yourmt3.py:forward()` 가
`bsz = 8` 로 `inference_file` 을 부른다. 즉 최고 사용량은 **파일 길이가 아니라
한 배치(세그먼트 8개)** 로 이미 정해져 있었다. 청크 6세그먼트는 그 배치보다
겨우 작아서 **거의 아무것도 아꼈지 못한다.** 앞 절의 "곡 길이에 따라 늘어난다"는
설명 자체가 틀렸다.

**(2) RSS 가 요청마다 올라간다.** 클립 하나를 돌리고 나면 유휴 RSS 가
**1298 MB → 1981 MB** 로 올라가 내려오지 않는다. `gc.collect()` 는 파이썬 객체를
버리지만 glibc 는 그 아레나를 붙들고 있다. 몇 요청 뒤엔 올라간 바닥값 + 한 번의
~5 GB 최고점이 11 GB 상자를 넘고, 커널이 워커를 고른다.

### 고친 방법: 청크가 아니라 **배치**를 줄인다

`bsz` 가 최고점을 정한다면 줄일 것은 그것이다. 어댑터의 `forward()` 본문은 못
바꾸지만 그 안에서 부르는 `model.inference_file` 을 감싸면 된다(`_cap_batch`).
청크보다 나은 이유가 있다: **세그먼트 경계도 그대로, 파일 전체를 한 번에 디코드
하는 것도 그대로**라 세그먼트를 건너 이어지는 음이 그대로 이어진다. 청크는 같은
방식으로 메모리를 묶지만 타이 병합이 넘지 못하는 경계를 하나 더 만든다.

band00 + band05, 2 클립 실측:

| 배치 세그먼트 | 최고 RSS | 시간 (초) | note F1 | +offset F1 | 원시 음 수 |
|---|---|---|---|---|---|
| 8 (백엔드 기본) | 7310 MB | 182 / 189 | 0.554 | 0.457 | 829 / 761 |
| 4 | 4302 MB | 201 / 189 | 0.554 | 0.457 | 829 / 761 |
| **2 (채택)** | **2696 MB** | **187 / 172** | 0.554 | 0.457 | 829 / 761 |
| 1 | 1651 MB | 201 / 236 | 0.554 | 0.457 | 829 / 761 |

**출력이 배치와 무관하게 완전히 동일하다** — 음 개수까지 같다. 세그먼트가 서로
독립이니 그래야 맞고, 곧 **이 캡은 공짜**라는 뜻이다. 속도도 안 떨어진다(배치 8이
182/189초, 배치 2가 187/172초로 잡음 범위). 배치 1은 20% 느려서 2로 정했다.

**7310 → 2696 MB (−63%), 정확도 손실 0.** 청크는 캡이 적용되지 않았을 때만 도는
폴백으로 남기고, `/health` 가 `batch_capped` 를 보고한다 — 이 가드는 동작할 때
눈에 보이는 효과가 없어서 조용히 깨질 수 있고, 깨지면 긴 곡 하나에 다시 죽는다.
GPU 이미지는 `MT3_DEVICE=cuda` 라 캡도 청크도 꺼진다(배치가 GPU 를 쓰는 이유다).

`malloc_trim(0)` 을 요청마다/언로드마다 부르게 했다(`_trim_heap`, glibc 아니면
무동작).

덤으로 **임시파일 누수**도 찾았다. `_to_pretty_midi` 가
`NamedTemporaryFile(delete=False)` 로 MIDI 를 쓰고 지우지 않는다 — 청크마다 하나씩.
`/tmp` 에 85개가 쌓여 있었다. 닫고 읽은 뒤 지우도록 고쳤다.

## 2026-09-05: 저음역 근본 원인의 예측을 검증하고 **베이스 구조 (bass rescue) 를 배포했다**

앞 절("근본 원인을 찾았다")의 예측 — 위로 리샘플하면 재현율 6% 구간이 모델의
home 음역으로 들어간다 — 을 실제로 돌려서 쟀다. band00–03 4클립에, +12 와 +7
반음으로 각각 리샘플한 실행을 만들고, 기존 3-시프트 앙상블 + 옥타브 보정 위에
컷오프(MIDI 36/42/48/54/60) 아래에서만 전조 실행을 단독으로 신뢰하도록 걸었다:

| 반음 | 컷오프 | P | R | F1 | 추가된 음 |
|---|---|---|---|---|---|
| (없음) | - | 0.809 | 0.730 | 0.766 | 0 |
| +7 | <36 | 0.800 | 0.764 | **0.779** | 110 |
| +7 | <42 | 0.788 | 0.766 | 0.774 | 142 |
| +7 | <48 | 0.758 | 0.776 | 0.764 | 247 |
| +12 | <36 | 0.803 | 0.744 | 0.770 | 51 |
| +12 | <42 | 0.790 | 0.748 | 0.766 | 85 |

**+7 반음이 모든 컷오프에서 +12 를 이겼다** — 작게 옮길수록 음 자체의 배음
구조가 덜 망가진다는 뜻이다. 컷오프는 36 이 가장 좋다: 재현율은 컷오프를 올릴수록
계속 늘지만(0.764 → 0.766 → 0.776) 정밀도가 그보다 더 빨리 깎여서(0.800 → 0.788 →
0.758) 순이익이 36 을 지나면 거꾸로 간다. 이건 예상과 맞는다 — MIDI 36 위는
이미 재현율 66%+ 구간이라(앞 절 표), 거기서 전조 실행 하나를 표 없이 통째로
믿으면 정밀도만 깎인다.

솔로 세트(ref00–05)에서 같은 +7 전조 실행을 독립으로 돌리면 **6클립 전부**
기본 실행보다 낮다(평균 note F1 0.917 대 0.939) — 솔로 피아노는 베이스가 이미
모델 음역 안에 있으니 이 실행이 배울 게 없고, 그래서 컷오프 아래에 추가될 새
음 자체가 거의 없다. 따라서 이 패스를 **솔로에서도 계속 돌릴 이유가 없다** —
분리해서 게이팅한다면 값은 공짜에 가깝지만, 지금은 켜져 있으면 켜지는 그대로
GPU 에서 매 곡마다 한 번 더 돈다.

### 배포한 것

4번째 **투표** 실행으로 넣는 방식(`ENSEMBLE_TRANSPOSE`, +1 반음, 이미 배포됨)과는
설계가 다르다는 걸 다시 확인해 둔다: 그건 비상관 관점 하나를 더해 표를 늘리는
것이고, 이번 건은 표를 늘리는 게 아니라 **표 자체가 성립할 수 없는 구간**(모든
실행이 똑같이 못 듣는 구간)에서 유일한 증인의 말을 그대로 받아들이는 것이다.
그래서 `runs`(투표 대상)에 넣지 않고 별도 패스로 뒀다.

`backend/app.py`:
- `_mt3_bass_rescue(rescue_notes, accepted, total_runs, cutoff)` — 신규.
  컷오프 아래·아직 채택되지 않은(±50ms 안에 같은 음높이가 없는) 음만 골라
  `agreement = total_runs` 로 얹는다. 슬라이더가 나중에 다시 나눠도(`_mt3_stems`)
  이 값이면 항상 살아남는다 — agreement 1 로 뒀으면 사용자가 기본으로 두는
  민감도에서 바로 다시 숨어 버렸을 것이다.
- `BASS_RESCUE_ON_GPU = 7`(반음), `BASS_RESCUE_CUTOFF = 36`, 둘 다
  `MUSICNOTE_MT3_BASS_RESCUE[_CUTOFF]` 로 덮어쓸 수 있다. GPU 에서만 기본으로
  켜진다 — 패스 하나를 더 도는 비용이라 CPU 폴백에서는 끈다(다른 전조/시프트
  실행들과 같은 원칙).
- `_mt3_pipeline` 순서: 앙상블 합치기 → 표 계산(`raw`) → 옥타브 보정(파트가 제
  음역에 있어야 "이미 채택됐다"는 판단이 맞다) → **베이스 구조** → 다이내믹스.
  베이스 구조가 다이내믹스보다 먼저인 건 새로 얹은 음도 음량을 읽어야 하기
  때문이다.

`eval/test_mt3_worker.py` 에 4개 테스트 추가: 새 저음이 실제로 얹히는지, 이미
채택된 음이 중복되지 않는지, 컷오프 이상/드럼은 건드리지 않는지.

`eval/audit_score.py::from_caches` 는 **적용하지 않는다** — 캐시된 참조 클립에
전조 실행 사본이 없어서, 이 경로를 통한 감사는 실제 파이프라인보다 저음역
재현율을 과소평가한다. 주석으로 남겨 둠.

## 2026-09-05: 컷오프·교차-전조 보강은 둘 다 불채택, 그리고 **더 큰 원인을 찾았다** — 음역이 아니라 음색

베이스 구조를 배포한 뒤, 새 추론 없이 **캐시만으로** 두 가지를 더 재보고, 그
다음 캐시에 이미 있던 신호(참조 MIDI의 GM 악기 번호)로 지금까지의 "음역" 설명
자체를 다시 검증했다.

**(1) 컷오프를 1반음 단위로 다시 쓸었다** — 원래 스윕은 36/42/48/54/60만 봤다.
32~38 사이 F1 은 0.779~0.780 으로 사실상 평평하다(잡음 수준의 차이). 36 을
바꿀 근거 없음.

**(2) +7 과 +12 전조 실행이 "둘 다" 동의할 때만 보강하면 어떨까** — 이미
캐시된 두 실행을 교차 검증에 썼다. 정밀도는 오른다(0.800→0.807)지만 재현율이
더 빠지고(0.764→0.743), 무엇보다 실행을 하나 더 돌려야 해서 **F1 이 오히려
낮다**(0.779→0.771). 불채택.

**(3) 그러다가 참조 MIDI 의 GM 악기 번호로 재현율을 다시 쪼개 보니, 지금까지의
"음역" 이야기가 교란되어 있었다.** 어쿠스틱 베이스와 일렉트릭 베이스는 거의
같은 음역(24-59)에 있는데:

| 악기 | 음역 | 음 수 | 재현율 |
|---|---|---|---|
| 어쿠스틱 베이스 | 36–47 | 340 | **92.1%** |
| 일렉트릭/신스 베이스 | 36–47 | 493 | 60.0% |
| 어쿠스틱 베이스 | 48–59 | 88 | **67.0%** |
| 일렉트릭/신스 베이스 | 48–59 | 135 | 19.3% |

**같은 음역인데 45~48%p 차이가 난다.** 그리고 결정적으로, "모델의 home
음역"이라던 60–71 구간에서도:

| 계열 | 음역 60–71 | 음 수 | 재현율 |
|---|---|---|---|
| 피아노 등("other") | 60–71 | 1353 | **94.6%** |
| 일렉트릭/신스 | 60–71 | 132 | **38.6%** |
| 일렉트릭/신스 | 72–83 | 343 | **39.1%** |

**"C4-B4 재현율 89.6%"라던 이전 결과는 그 구간 음의 대다수가 피아노였다는
사실에 오염돼 있었다.** 일렉트릭 베이스·신스 베이스·신스 패드·신스 리드·드로바
오르간·디스토션 기타는 음역과 무관하게 전부 낮다(악기별 집계, 8클립 전체):

| 악기 | 음 수 | 재현율 |
|---|---|---|
| 어쿠스틱 그랜드 피아노 | 232 | 100.0% |
| 어쿠스틱 베이스 | 343 | 83.7% |
| 슬랩 베이스 | 99 | 85.9% |
| **일렉트릭 베이스(핑거)** | 447 | **34.7%** |
| **신스 베이스 1/2** | 247 | **~41%** |
| **튜바** | 265 | **29.1%** |
| **신스 패드** | 204 | **33.8%** |
| **신스 리드** | 134 | **32.8%** |
| **드로바 오르간** | 98 | **27.6%** |
| 디스토션 기타 | 67 | 70.1% |

**이건 청음 한계(psychoacoustic)가 아니라 학습 데이터의 악기 분포 문제로
읽힌다.** 어쿠스틱 베이스는 낮은 음역에서도 83~92% 재현하는데 일렉트릭/신스
베이스는 어느 음역에서도(24-35 는 물론 60-83 까지) 30-40% 대에 갇혀 있다 —
음정이 안 들려서가 아니라, MT3 가 그 음색 자체를 잘 인식하지 못하는 것이다.
지금까지 배포한 옥타브 보정·베이스 구조는 이 데이터셋의 저음역 악기가 우연히
전자/신스 계열이 많아서 효과가 있었을 가능성이 크고, **음역이 아니라 음색을
표적으로 하는 접근이 남은 격차의 더 큰 부분을 설명할 것**이다.

### 다음으로 시도할 만한 것 (아직 안 함 — 새 계산 필요)

음색 문제는 피치를 옮기는 트릭(전조·앙상블)으로 고치기 어렵다 — 모델이 그
악기를 "그런 소리로" 학습하지 못한 것이라서다. 더 유망한 방향은 stems.py 가
이미 갖고 있는 **Demucs bass 스템 분리**로 일렉트릭 베이스를 섞인 믹스에서
꺼낸 뒤, 그 격리된 신호에 MT3(또는 CREPE/pYIN 같은 단성 피치 트래커)를 새로
돌려 보는 것 — 신호가 깨끗해지면 같은 모델도 더 잘 인식할지, 아니면 음색
문제가 신호 대 잡음비가 아니라 순전히 인식 실패인지 가른다. 이건 새 추론이
필요하다(Demucs 분리 + 스템 위 MT3 재실행) — 이번 세션에서는 아직 테스트하지
않았다.

## 2026-09-05: 후처리 대신 **더 나은 모델**을 찾아 직접 재봤다

사용자 지시: 후처리 트릭 말고, 음색과 박자를 잘 인식하는 **모델 자체**나
**모델을 잘 쓰는 방법**으로 풀어보라. 넷을 실측했다.

### (1) `mr_mt3` 와 `mt3_pytorch` 는 사실 동일 체크포인트다

지금 쓰는 `mt3-infer` 툴킷의 두 백엔드 checkpoints.yaml 에 적힌 sha256 이
바이트 단위로 같다. 업스트림(kunato/mt3-pytorch)의 git-lfs 포인터 파일을 직접
받아 대조해 독립적으로 재확인함 — 캐싱 버그가 아니라 실제로 같은 가중치다.
"mt3_pytorch 로 바꾸면 더 정확해진다"는 옵션은 존재하지 않는다.

### (2) YourMT3 (536MB, GPU) — 이미 받아져 있던 걸 실제로 채점

**바로잡음**: 이 절을 처음 쓸 때 "우리가 이미 배포한 mr_mt3 파이프라인"이라고
세 군데 적었는데 틀렸다. `ecosystem.config.js` 와 캐시된
`eval/refs_band/*.mt3.json` 의 `"model"` 필드를 확인하니 pm2 로 실제 떠 있는
`mt3-worker` 는 처음부터 `MT3_MODEL=yourmt3` 였다 — 이번 세션 내내 옥타브
보정·베이스 구조·레지스터 재현율 표를 만든 기준 파이프라인은 전부 **YourMT3
위에 앙상블+후처리를 얹은 것**이었지 mr_mt3 가 아니다. 아래 비교는 그래서
"YourMT3 단독" 대 "YourMT3 + 우리 앙상블·후처리"로 다시 읽어야 맞다 —
숫자 자체는 바뀌지 않는다.

단일 패스, 앙상블·옥타브 보정 없이 refs_band + refs_solo 전체에 실측:

| | onsetF1 | noteF1 |
|---|---|---|
| 밴드 8클립 평균 | 0.822 | 0.650 |
| 솔로 6클립 평균 | 0.918 | 0.941 |

솔로는 훌륭하지만 밴드는 우리가 이미 배포한 YourMT3 앙상블(옥타브 보정+베이스
구조, F1 0.734+)보다 **낮다** — 즉 그 앙상블·후처리 장치들이 진짜 값을
하고 있다는 뜻이기도 하다. 그리고 음색으로 쪼개보면:

| 계열 | 재현율 (YourMT3 단독) |
|---|---|
| 어쿠스틱 | 66.5% |
| 전자/신스 | **35.8%** |

우리 YourMT3 앙상블 파이프라인(전자/신스 37.8%)과 사실상 같은 결함을 그대로
가지고 있다. 앙상블·후처리를 아무리 얹어도 이 축은 못 고쳤다는 뜻이고,
YourMT3 를 (더 크게) 바꿔 쓰는 것도 정답이 아니다.

### (3) MuScriptor (Kyutai + Mirelo, 2026-08 공개, arXiv 2607.08168) — 결정적 신호

`pip install muscriptor`, decoder-only transformer, 3사이즈(small 103M /
medium 307M / large 1.4B). 학습 데이터에 electric bass · distorted guitar 등
음색을 명시 라벨로 포함하고, 어휘 자체가 `acoustic_bass`/`electric_bass`,
`clean_electric_guitar`/`distorted_electric_guitar`, `synth_lead`/`synth_pad`
를 구분한다 — 우리가 겪는 정확히 그 구분이다. **가중치는 CC BY-NC 4.0
(비상업적 용도만), 코드는 MIT. HuggingFace 게이트라 크기별로 따로 라이선스
동의가 필요.**

**small (103M, CPU 단일 패스)** 로 refs_band + refs_solo 전체 실측(앙상블도
후처리도 없음):

|  | onsetF1 | noteP | noteR | noteF1 |
|---|---|---|---|---|
| 밴드 8클립 평균 | 0.779 | 0.737 | 0.612 | **0.700** |
| 솔로 6클립 평균 | 0.845 | 0.788 | 0.727 | 0.738 |

밴드 평균 F1 0.700 은 우리 전체 파이프라인(다중 패스 + comb-NMF 옥타브 보정 +
레지스터 게이트 베이스 구조, 0.734~0.745)에 **단일 CPU 패스로 근접** — 후처리
장치가 하나도 없는 채로다. 그리고 음색으로 쪼개면 이게 핵심이다:

| 계열 | YourMT3 (배포된 파이프라인) | MuScriptor small (단일 패스) |
|---|---|---|
| 어쿠스틱 | 84.2% | **91.9%** |
| 전자/신스 | 37.8% | **65.8%** |

**전자/신스 재현율이 37.8% → 65.8%, 74% 상대 개선.** 어쿠스틱도 같이 오른다 —
트레이드오프가 아니라 두 축 다 낫다. 이게 이번 세션이 찾던 답에 가장 가깝다:
후처리로는 못 건드리는 축(모델이 그 음색 자체를 학습했는가)을 정면으로
해결한다.

**단, 솔로에서는 후퇴한다.** ref00~05 개별: 0.620~0.881, 평균 0.738 — 우리
현재 솔로 파이프라인(0.92~0.99)보다 뚜렷이 낮다. 하나의 모델이 전부를
이기는 게 아니라, **밴드에서 이기고 솔로에서 진다.**

**박자: 별도로 재보니 오히려 후퇴.** MuScriptor 가 물고 있는 `beat_this`
(ISMIR 급 신경망 비트 트래커)를 감싼 `detect_grid()` 는 "일정한 템포"를
전제하고 안 맞으면 그냥 실패를 던진다 — refs_meter 13클립 중 **7개가 아예
실패**(비트가 고정 템포에서 89~738ms RMS 벗어남), 남은 6개도 beatF1 0.825 지만
downbeatF1 **0.273** (0/13 다운비트 중 여러 개가 0.000, 템포를 정확히 2배/절반
으로 잘못 잡은 경우도 3건). 우리가 이미 쓰는 `meter.detect()` 는 13클립 전부
답을 내고 beatF1 0.868 / downbeatF1 0.592 — **박자는 우리 쪽이 낫다.**

**medium(307M, 논문 기본 설정)은 아직 못 쟀다** — small 과 별개로 HF 게이트
승인이 필요한데, medium 저장소는 자동 승인이 아니라 수동 심사 대상이었다
(403, "not in the authorized list"). small 의 솔로 후퇴가 모델 크기 때문인지
(medium/large 가 고치는지) 아직 미확인.

### 결론과 다음 결정

**음색은 모델을 바꿔서 푸는 게 맞다** — 후처리(옥타브 보정, 레지스터 게이트
구조)로 만든 개선폭보다 MuScriptor 로의 전환이 electric/synth 축에서 훨씬
크게 이긴다. 다만 한 모델로 전부 교체가 아니라 **콘텐츠별로 나누는 하이브리드**
가 맞는 그림으로 보인다: 밴드/믹스 채보는 MuScriptor, 솔로 피아노와 박자
검출은 지금 파이프라인 그대로. 실제 프로덕션 반영 전에 남은 것:

1. medium/large 가 솔로 후퇴를 고치는지 (HF 승인 필요, 사용자 조치 필요할 수
   있음)
2. CC BY-NC 라이선스가 MusicNote 의 실제 사용 방식과 맞는지 (상업적 용도
   계획이 있다면 이 가중치는 그대로 못 씀)
3. MuScriptor 출력(NoteStartEvent/NoteEndEvent, 자체 악기 어휘)을 지금
   ScoreDoc/앙상블 파이프라인에 연결하는 통합 작업

아직 코드에는 반영하지 않았다 — 위 세 가지가 결정되기 전에 배포하기엔
이르다는 판단.

### 후속: 라이선스는 문제 없음, medium 은 솔로 후퇴를 못 고친다

**(2) 는 해소.** MusicNote 는 상업화 계획이 없다고 사용자가 확인 — CC BY-NC
가중치를 그대로 쓸 수 있다.

**(1) medium(307M) 을 대표 클립 4개로 재본 결과:**

| 클립 | small noteF1 | medium noteF1 | 시간(작음→중간) |
|---|---|---|---|
| band06 (최악 음색) | 0.544 | 0.526 (오히려 조금 낮음) | 43.7s → 182.2s |
| band04 (양호) | 0.891 | 0.964 | 80.5s → 176.0s |
| ref00 (솔로) | 0.666 | 0.717 | 106.5s → 133.6s |
| ref03 (솔로) | 0.620 | 0.736 | 57.8s → 143.4s |

솔로가 조금 오르긴 하지만(0.62~0.67 → 0.72~0.74) 우리 현재 파이프라인의
0.92~0.99 에는 한참 못 미치고, 밴드 최악 클립에서는 크기를 키워도 개선이
없다 — 파라미터 부족이 아니라 학습 분포(밴드/믹스에 특화, 클린 솔로 피아노는
비중이 작음)의 특성으로 보인다. 연산은 4.2배 늘었는데 대가가 이 정도라 large
(1.4B) 를 CPU 로 더 재는 건 비용 대비 근거가 약함 — GPU 를 다시 빌려서까지
확인할 값어치는 아직 없다고 판단, 보류.

**결론이 바뀐다: 하이브리드 형태 그대로 진행.** medium/large 로 스케일을
올려서 하나의 모델로 통일하는 길은 막힌 것으로 보고, 남은 건 (3) 통합
작업뿐이다.

### large(1.4B) 까지 실측 — 사용자 요청으로 GPU 를 다시 빌려 확인

CPU 로 large 를 돌리다 이 박스(11GB, 이 프로젝트 말고도 다른 서비스 여럿이
같이 떠 있음) 커널 OOM 으로 프로세스가 죽었다(RSS 8.8GB). 다른 서비스는
멀쩡했지만(커널이 메모리를 제일 많이 먹은 프로세스만 정확히 골라 죽임),
로컬에서는 안전하게 돌릴 수 없다고 판단해 vast.ai GPU(RTX A4000, 16GB, 약
$0.09/시간)를 새로 빌려 확인했다.

| 클립 | small | medium | large |
|---|---|---|---|
| band06 (최악 음색) | 0.544 | 0.526 | 0.542 |
| band04 (양호) | 0.891 | 0.964 | 0.902 |
| ref00 (솔로) | 0.666 | 0.717 | 0.727 |
| ref03 (솔로) | 0.620 | 0.736 | 0.754 |

**세 크기에 걸쳐 패턴이 확정됐다**: 솔로는 커질수록 아주 조금씩 오르지만
0.73 근처에서 사실상 멈추고(우리 파이프라인의 0.92~0.99 에는 안 닿는다),
가장 심각했던 밴드 클립(band06)은 103M 에서 1.4B 까지 가도 전혀 안 움직인다
(0.544 → 0.526 → 0.542, 사실상 잡음). **모델을 키운다고 풀리는 문제가
아니라는 게 이제 확정적이다** — 학습 분포 자체의 특성이라는 앞선 가설과
일치한다. 배포에는 **small** 을 그대로 쓴다: 나머지 두 크기가 사는 값이
없다.

GPU 는 테스트 끝나자마자 반납했다(`vastai destroy instance`).

### 통합: MuScriptor small 을 타깃 보강 패스로 배포

`mt3_worker.py`/`mt3_bridge.py` 와 같은 모양으로 새 워커·브리지 쌍을 추가했다
— muscriptor 의 의존성 트리(다른 torch 버전)가 mt3-infer 나 메인 앱 venv 를
건드리지 않도록:

- `backend/muscriptor_worker.py` — 자체 venv(`~/muscriptor-venv`)에서 도는
  HTTP 서버. `mt3_worker.py` 와 같은 `/health`·`/transcribe` 모양을 그대로
  따른다. MuScriptor 자체 음색 어휘(`get_group_program_map`)로 대표 GM
  program 번호도 같이 매겨서, `instrument`(세부 음색 라벨) 필드 하나만
  추가하고 나머지는 기존 다운스트림 코드(voices.py, musicxml.py 등)가
  이해하는 스키마 그대로 유지한다.
- `backend/muscriptor_bridge.py` — 클라이언트. 원격 GPU 경로는 아직 없다 —
  small 모델이 이 CPU 에서도 거의 실시간이라 필요가 없었다.
- `backend/app.py::_muscriptor_timbre_rescue` — 베이스 레지스터 구조
  (`_mt3_bass_rescue`)와 같은 패턴이지만 게이트가 **음역이 아니라 음색
  라벨**이다(`MUSCRIPTOR_TIMBRES = electric_bass, organ, synth_lead,
  synth_pad, tuba` — 우리 파이프라인이 실측으로 약한 걸 확인한 것만). 이미
  채택된 음과 겹치지 않는 것만, agreement 를 총 실행 수로 얹어서 추가한다.
  옥타브 보정 뒤·다이내믹스 앞에 끼운다. GPU 게이트는 없음(패스 자체가
  싸다) — `MUSICNOTE_MUSCRIPTOR_RESCUE=0` 으로 끌 수 있다.
- `ecosystem.config.js` 에 `muscriptor-worker` pm2 앱 추가, `small` 고정.
  medium/large 를 안 쓰는 이유는 바로 위 절 참조.
- `eval/test_muscriptor_rescue.py` — 실제 워커 없이 게이팅/중복 방지/장애
  허용을 검증하는 6개 테스트.

HF 게이트 인증은 pm2 의 `env` 블록이 아니라 `huggingface_hub.login()` 으로
`~/.cache/huggingface/` 표준 캐시에 한 번 써 뒀다 — 토큰을 커밋되는 설정
파일에 두지 않기 위해서다.
