# 정확도 개선 작업 지시서

`CLAUDE_CODE_HANDOFF.md`(codex 작성)를 읽고, 실제로 측정한 뒤 정리한 문서다.
핵심 결론: **codex가 만든 평가 하네스는 한 번도 실행된 적이 없고, 그동안의
정확도 판단은 Canon 클립 한 개의 육안 관찰에 근거하고 있었다.**
아래 숫자는 2026-08-30에 `eval/refs`(6클립, 3281음) 로 처음 측정한 값이다.

## 0. 측정된 기준선 (baseline)

### 0-1. 중요한 정정: 두 경로는 서로 다른 엔진이다

처음에 `direct:polyphonic` 으로 잰 값(note F1 0.526)은 **출시 경로가 아니다.**
`eval_harness.py --engine direct:` 는 `transcribe.py` 의 basic-pitch 를 부르는데,
사용자가 `polyphonic` 을 고르면 `app.py:519` 의 `is_mt3` 때문에 **MT3** 가 돈다.
아래 숫자는 실제 MT3 출력으로 다시 잰 것이다.

### 0-2. MT3 경로 (실제 출시 경로)

`python eval/replay_eval.py eval/refs` — MT3 raw 출력 캐시를 재생해서 채점.
6클립 전부 캐시 완료 (2026-08-30).

| 지표 | 값 |
|---|---|
| onset F1 | 0.958 |
| **note F1** | **0.947** |
| note+offset F1 | 0.869 |
| note precision / recall | 0.974 / 0.922 |

클립별: ref00 0.865 · ref01 0.968 · ref02 0.986 · ref03 0.951 ·
ref04 0.934 · ref05 0.979.

게이트 수정 전 같은 경로는 ref00 에서 note F1 **0.175** 였다.

### 0-3. 참고: basic-pitch 경로 (출시 안 됨)

onset F1 0.717 / note F1 0.526 / note+offset F1 0.086,
precision 0.818 vs recall 0.650. 라이브러리 모드용 참고치로만 유지한다.

### 이 숫자가 말하는 것

1. **MT3 자체는 나쁘지 않았다.** 게이트를 끄면 note F1 0.865, note+offset F1
   0.811 이 나온다. 문제는 모델이 아니라 후처리였다.
2. **문제는 과검출이 아니라 미검출이었다.** precision 0.93 / recall 0.81.
   그리고 그 미검출의 대부분은 아래 P1 한 줄에서 나왔다.
3. **basic-pitch 로 관찰한 "음 길이가 2.9배 길다"는 MT3 에 해당하지 않는다.**
   MT3 는 오히려 짧게 낸다(중앙값 30 ms, 정답 45 ms). 원래 P2 로 잡았던
   offset 정제는 출시 경로에서는 우선순위가 크게 낮아진다. 8절 참조.

---

## 1. [P0] 기준선을 회귀 게이트로 고정한다 — 완료

**문제였던 것**: `backend/eval_harness.py` 와 `eval/refs` 가 갖춰져 있는데도
실행 기록이 저장소 어디에도 없었다. 최근 세 커밋은 모두 이 지표에 어떤
영향을 줬는지 확인되지 않은 채 들어갔다.

**한 일**
- `backend/mt3_post.py` (신규): `app._mt3_stems` 에 인라인으로 있던 게이트를
  순수 함수로 분리. env 로 오버라이드 가능하고, **몇 개를 왜 지웠는지
  리포트를 반환**한다. 이전에는 조용히 사라졌다.
- `eval/mt3_cache.py` (신규): 클립당 MT3 raw 출력을 한 번만 캐시한다.
  이 서버에서 MT3 는 실시간의 약 13배라 매번 추론하면 튜닝이 불가능하다.
- `eval/replay_eval.py` (신규): 캐시를 읽어 게이트 + 시퀀스 분리를 재생하고
  채점한다. `--sweep-min-note` 로 파라미터 스윕. **초 단위로 돈다.**
- `eval/test_mt3_gate.py` (신규): 게이트 회귀 테스트.
- `eval_harness.py` docstring 에 direct/api 엔진 차이 경고 추가.

**하네스 자체 검증**: 정답 MIDI 를 그대로 MT3 출력인 척 주입하면
(= 완벽한 채보) note F1 0.992 가 나온다. 채점 배관이 맞다는 뜻이다.

**남은 것**: 정확도에 영향을 주는 변경은 전후 note F1 을 같이 적는다.
올랐는지 내렸는지 모르는 변경은 병합하지 않는다.

---

## 2. [P1] 80 ms 최소 길이 필터 — 완료, 영향이 가장 컸다

**문제**: `backend/app.py:377-382` (수정 전)

```python
amp_thr = int(round(18 * (1.0 - s)))   # s=0.5 -> 9
min_len = 0.05 + 0.06 * (1.0 - s)      # s=0.5 -> 0.08 s
if n["velocity"] < amp_thr or (n["end"] - n["start"]) < min_len:
    continue
```

MT3 는 프레임 피크가 아니라 **이산 노트 이벤트**를 낸다. ref00 에서 MT3 raw
음 길이 중앙값은 30 ms, 88.9 % 가 80 ms 미만이다. 정답도 중앙값 45 ms,
86.4 % 가 80 ms 미만이다 — 즉 짧은 음이 정상이다.
그런데 이 게이트는 **610 음 중 542 음(89 %)을 지우고 있었다.**

결과: note F1 0.175. 완벽한 채보를 넣어도 이 게이트로는 0.564 가 상한이었다.

**추가 발견**: YourMT3 는 **모든 음의 velocity 를 100 으로 고정**해서 낸다.
따라서 velocity 게이트는 한 번도 발동한 적이 없고, 감도 슬라이더는 MT3
경로에서 **오직 짧은 음을 지우는 기능만** 하고 있었다.
`app.py` 의 `conf = 0.55 + 0.4*(velocity/127)` 도 전 음 동일한 상수라
`low_conf` 표시는 의미가 없다.

**한 일**
- 길이 하한을 0.005 s 고정으로 낮췄다(0 ~ 0.008 구간은 점수가 동일).
  길이는 이제 필터가 아니라 **퇴화 이벤트 방어용 검사**다.
- 감도가 길이를 건드리지 못하도록 `MIN_NOTE_SPAN = 0.0` 으로 분리했다.
- 게이트가 버린 개수를 로그로 남긴다.

**결과**: ref00 note F1 **0.175 → 0.865**, note+offset F1 0.152 → 0.811.

**남은 결정**: MT3 경로에서 감도 슬라이더는 이제 아무 효과가 없다.
MT3 가 per-note confidence 를 주지 않기 때문이다. 길이로 대체하면 안 된다
(그게 이 버그였다). 슬라이더를 MT3 모드에서 숨기거나, 별도의 신뢰도
추정을 붙이는 결정이 필요하다. **사용자 판단 필요.**

---

## 3. [P2] 음 길이(offset) 정제 — 우선순위 강등

원래 note+offset F1 0.086 을 근거로 P2 로 올렸으나, 그 값은 basic-pitch
경로의 것이었다. **MT3 경로는 게이트 수정 후 0.811 이다.**

다만 MT3 는 음을 짧게 내는 경향이 있다(20 ms 미만이 35.7 %, 정답은 1.9 %).
mir_eval 의 offset 허용치가 짧은 음에서는 최소 50 ms 로 동작하므로 지표에
잘 안 잡힐 뿐, 악보 표기에서는 문제가 될 수 있다.

**할 일 (낮은 우선순위)**
- 악보용 최소 표기 길이는 채점용 offset 과 분리해서 다룬다.
- `voices.py:91` 의 0.18 s 초과 허용은 연결 판단에만 쓰고 기록되는 `end`
  에는 반영하지 않도록 분리한다.

---

## 4. [P3] 시간 이동 앙상블 — 구현 완료, 측정 대기

**문제였던 것**: `mt3_worker.py` 는 파형 전체를 한 번만 추론했고, codex 는
끝부분 누락에 대해 오른쪽 0.75 s 패딩만 추가했다(`END_PADDING_SECONDS`).
이건 **클립 끝** 문제만 다룬다. 곡 중간 누락은 모델 내부 세그먼트 경계에서
생기므로 패딩으로는 손댈 수 없다.

**한 일**
- YourMT3 설정을 확인했다: `input_frames = 32767` = **2.048 s 비중첩
  세그먼트**. 따라서 이동량은 임의값이 아니라 **반 세그먼트 = 1.024 s** 다.
  이전 경계가 전부 세그먼트 한가운데로 이동한다.
- `mt3_worker.transcribe(..., shift=)`: 앞에 무음을 덧대 추론하고 방출
  시각에서 다시 빼서 원래 타임라인으로 되돌린다. 왕복 검증 완료.
- `backend/mt3_ensemble.py`: 두 실행을 병합하고 음마다 `agreement` 를 붙인다.
  **YourMT3 는 velocity 를 전부 100 으로 내므로 이것이 유일한 per-note
  신뢰도다.**
- **전달과 검토는 별개 결정**이다. 처음엔 한 함수로 묶었는데, 그러면
  union 모드에서 전부 전달되어 검토 큐가 비어 목적이 사라진다.
  전달은 `agreement >= min_agreement`, 검토는 `agreement < 실행 수`.
  각 항목의 `in_score` 로 "악보에 있음 · 확인 필요" 와 "누락 의심 · 승인 시
  추가" 를 구분한다. 자동 반영은 없다.

**남은 것**: union(`min_agreement=1`) vs vote(2) 중 무엇이 note F1 이 높은지
아직 측정 못 했다. shift 캐시 생성 중. 현재 기본값 union 은 잠정값이다.

**비용**: MT3 를 2회 돌리므로 시간이 2배다. 다만 기존에도 매 작업마다
piano-tx 를 돌리고 있었으므로 프로파일은 비슷하다 — 작동하지 않는 쪽에서
작동하는 쪽으로 옮긴 것이다.

---

## 5. [P4] Demucs 와 MT3 가 서로 배타적이다

**문제**: `backend/app.py:524` — `use_demucs = mode == "stems"`.
MT3 경로는 원본 믹스를 그대로 받는다. 혼합 음원에서 스템 분리 후 채보는
표준적인 정확도 향상 수단인데, 지금은 둘 중 하나만 쓴다.
codex 문서에도 이 조합에 대한 언급이 없다.

**할 일**
- 실험 플래그로 `demucs → 스템별 MT3 → 병합` 경로를 만든다.
- 하네스로 `api:mt3` vs `api:mt3+demucs` note F1 을 비교한다.
- 메모리 슬롯(`_STEMS_SLOT`/`_MT3_SLOT`)이 이미 직렬화돼 있으므로
  순차 실행은 가능하다. 대신 소요 시간이 크게 늘어난다.

**완료 기준**: 밴드/보컬 포함 클립에서 note F1 향상이 확인되면
해당 음원 유형에 한해 기본 경로로 승격.

---

## 6. [P5] 누락 검증기 — 완료, 접근 자체를 교체했다

**측정 결과** (`eval/eval_validator.py`):

| clip | 전달된 음 | 실제 누락 | 후보 보고 | 맞은 것 | precision | recall |
|---|---|---|---|---|---|---|
| ref00 | 610 | 132 | 15 | 0 | 0.000 | 0.000 |
| ref01 | 745 | 30 | 5 | 0 | 0.000 | 0.000 |

버그가 아닌지 직접 확인했다. ref00 의 실제 누락은 1.02~24.82 s / MIDI 30~103
에 132 개 퍼져 있는데, 보고된 15 개는 5.24~23.02 s / MIDI 41~74 에 몰려 있고
**하나도 겹치지 않는다.** 후보 중 6 개가 pitch 66 하나에 반복해 찍힌다 —
실제 음이 아니라 공진/배음을 잡고 있다는 신호다.

**구조적 한계**
1. 스캔 범위가 `lo, hi = 40, 84` 라 실제 누락의 **44 % 가 원리적으로 범위
   밖**이다 (ref00 은 48 % 만 범위 안).
2. 2차 확인이 `family="keyboard"` → **piano-tx 전용 모델을 통째로 한 번 더**
   돌린다. piano-tx 자체 note F1 은 0.560 으로 MT3 의 0.947 보다 한참 낮다.
   더 나쁜 모델로 더 나은 모델을 검증하고 있었다.

확인 단계는 별도로 완주하지 않았다. 후보 집합을 **거르는** 단계이므로
precision 0 인 집합에서 유의미한 결과가 나올 수 없고, 비용만 크다.

**한 일**: 후보 출처를 P3 앙상블 불일치로 교체했다. 같은 모델이 같은
오디오를 본 결과라 독립적인 오검출 모드가 없다. CQT 경로는
`MUSICNOTE_CQT_CANDIDATES=1` 로 옵트인 보존해 비교 재현이 가능하다.

**교훈**: CQT 에너지는 **지속음과 새 어택을 구분하지 못한다.** Canon 클립의
gap 조사에서도 "가장 강한 음"이 대부분 방금 친 음이 계속 울리는 것이었다.
이것이 precision 0 의 근본 이유다.

---

## 7. [P6] 시퀀스 분리 — 버그 3개 수정 완료

### 7-1. 체인 병합 (stage 2) — 완료

`separate_sequences` 가 쌍별 점수를 낸 뒤 재확인 없이 union 했다. 병합은
전이적이라 한 번도 비교되지 않은 그룹이 연결된다: 옥타브씩 떨어진 세 선율이
A-B, B-C 는 통과하고 A-C 는 거부되는데도 셋이 한 시퀀스(28 반음)가 됐다.
모듈이 문서화하고 쌍마다 강제하는 옥타브 규칙을 출력이 위반했다.

첫 수정(모든 교차 쌍 통과 요구)은 `one_off_extension` 을 깨뜨렸다.
`None` 이 **두 가지**를 의미했기 때문이다:

| 상황 | 의미 | 처리 |
|---|---|---|
| 공유 어택 충분 + 음역/간격 실패 | 진짜 충돌 | 거부권 |
| 공유 어택 부족 | 판단 불가 | 기권 |

`_chord_verdict` 가 이제 셋을 구분하고 **실제 충돌만** 병합을 막는다.

### 7-2. 겹침 거부권 (stage 1) — 완료

사용자 제보(13 초 클립, 멜로디가 4 성부 중 3 개로 삼등분)에서 발견.
그 실행의 voice 별 MIDI 를 합치면 `separate_sequences` 의 입력이 그대로
복원되므로 MT3 재추론 없이 재현했다.

MT3 는 이 클립에서 멜로디 음을 다음 어택보다 **0.32 / 0.58 / 1.32 s** 길게
잡았다(어택 간격 0.20 s). 그런데 겹침 0.18 s 초과면 그 contour 를 후보에서
아예 제외했다. 방금 연주한 선율이 자기 연속을 못 잇고 새 contour 가 열리고,
다시 원래 것이 풀리면 되돌아온다 — 단선율 멜로디가 a1↔a2 를 왕복했다.
이 함수가 막겠다고 명시한 바로 그 현상이다.

겹침을 **상한 있는 약한 가중치**로 바꾸고, 선율이 이어질 때 이전 음의 끝을
새 어택 시점으로 자른다(악보의 한 성부는 자기 자신과 겹칠 수 없다).
MT3 의 과도한 지속이 악보로 새는 것도 함께 막힌다.

### 7-3. 옥타브 도약 = 새 성부 (stage 1) — 완료

새 contour 를 여는 비용이 12.0 고정인데 도약 비용은 반음 개수 그대로였다.
즉 **옥타브 도약이 "새 연주자 등장"과 동일 비용**이라 모든 옥타브 점프가
성부를 하나 만들었다. Canon 멜로디는 정확히 그 두 도약 지점에서 잘렸다.
`_leap_cost` 가 옥타브 변위를 음정 클래스가 같은 가까운 이웃으로 다룬다.
진짜 먼 도약(19 반음 이상)은 여전히 새 성부를 연다.

### 7-4. 결과, 그리고 두 증상이 하나였다

Canon 클립 **4 → 3 시퀀스**. 남은 분할은 t=3.24 의 59, t=4.04 의 62 —
사용자가 "들리지 않는다"고 한 그 음들이 세 번째 contour 를 열고 멜로디가
거기로 옮겨간 것이었다. **그 두 음만 빼면 정확히 2 시퀀스**:
54 음 멜로디(0.11~12.54 s 연속) + 14 음 베이스. 유령 음과 쪼개진 멜로디는
별개 문제가 아니라 하나다.

그 두 음이 실제인지는 **추측하지 않는다**. 앙상블로 판정할 일이다.

### 7-5. 남은 것

실제 오디오 시퀀스 회귀가 없다. `eval/refs` 에는 음만 있고 "이 음들이 한
성부"라는 정답 레이블이 없어서 note F1 로는 잡히지 않는다(시퀀스 분리는
음 집합을 보존한다). 사람이 매긴 성부 정답이 필요하다.

---

## 8. [P7] 레퍼런스 세트가 제품과 다르다

**문제**: `eval/refs` 6클립은 전부 MAESTRO 피아노 독주를 fluidsynth 로
렌더한 것이다. `eval/build_refs.py` 주석도 "실제 녹음보다 깨끗해서 F1이
낙관적으로 나온다"고 스스로 밝히고 있다. 그런데 제품의 주 사용처는
YouTube Shorts / 밴드 / 보컬이다. 즉 **지금 숫자는 상대 비교용으로만 유효하다.**

**할 일**
- 실제 녹음 기반 세트를 추가한다 (MAPS/MAESTRO 실연 녹음, Slakh 등).
- 최소한 피아노 독주 / 보컬+반주 / 밴드 3개 카테고리로 나눠 평균을 낸다.
- 카테고리별로 note F1 을 따로 본다. 하나의 평균은 회귀를 감춘다.

**완료 기준**: 소프트폰트 세트와 실녹음 세트의 note F1 차이를 알고 있고,
튜닝이 실녹음 쪽을 기준으로 이뤄진다.

---

## 작업 순서 요약

| 순 | 항목 | 상태 | 효과 |
|----|------|------|------|
| 1 | 기준선 고정 (P0) | **완료** | 이후 모든 판단의 전제 |
| 2 | 80 ms 게이트 (P1) | **완료** | note F1 0.175 → 0.865 (ref00) |
| 3 | 검증기 교체 (P5) | **완료** | precision 0.000 → 앙상블 기반 |
| 4 | 시퀀스 분리 버그 3종 (P6) | **완료** | Canon 4 → 3 시퀀스 |
| 5 | 앙상블 union vs vote | **측정 중** | 기본값 union 은 잠정 |
| 6 | 감도 슬라이더 처리 | **판단 필요** | MT3 경로에서 무효 |
| 7 | 실제 오디오 성부 회귀 (P6-5) | 대기 | 성부 정답 레이블 필요 |
| 8 | Demucs+MT3 (P4) | 대기 | 혼합 음원 |
| 9 | 실녹음 세트 (P7) | 대기 | 측정 신뢰도 |
| 10 | offset 정제 (P2) | 강등 | MT3 는 이미 0.869 |

## 재현 명령

```bash
# MT3 raw 출력 캐시 (클립당 약 5분, 최초 1회만)
python eval/mt3_cache.py eval/refs
python eval/mt3_cache.py eval/refs --shift 1.024   # 앙상블용 2차 실행

# 후처리 채점 — 초 단위
python eval/replay_eval.py eval/refs
python eval/replay_eval.py eval/refs --sweep-min-note 0.005,0.02,0.08
python eval/replay_eval.py eval/refs --shifts 1024 --min-agreement 1

# 검증기 자체 측정
python eval/eval_validator.py eval/refs --stage candidates

# 회귀 테스트
python eval/test_mt3_gate.py
python eval/test_mt3_ensemble.py
python eval/test_sequence_analysis.py

# 전체 파이프라인 (느림: MT3 는 실시간의 약 13배)
python backend/eval_harness.py --engine api:mt3 eval/refs
```
