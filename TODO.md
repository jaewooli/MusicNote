# MusicNote 다음 작업

## 1. 시퀀스 분리의 실제 음원 검증

- 짧은 피아노·보컬·밴드 클립으로 회귀 세트를 만든다.
- 동시음이 한 화음인 경우에는 하나의 시퀀스로 남는지 확인한다.
- 반주 화음과 독립 멜로디가 전후로 이어질 때에만 둘 이상의 시퀀스로 갈라지는지 확인한다.
- MT3가 만든 악기 track은 유지하고, 시퀀스는 그 track 내부의 악보 해석으로만 사용한다.

완료 기준: 각 테스트 클립에서 음 누락 없이, 사람이 납득할 수 있는 화음/멜로디 묶음이 나온다.

## 2. 악보·피아노 롤·오디오 시간축 통일

- 편집기에도 결과 화면과 동일한 ScoreDoc 기반 다성부 렌더러를 사용한다. (남음)
- 기존 편집기 렌더러의 “각 시점 최고음만 표시” 동작을 제거한다. (남음)
- ~~악보 마디의 실제 시작/끝 시간과 재생 바·시크 동작을 회귀 테스트한다.~~
  재생 선은 이제 실제로 그려진 음표 위치(`Score._layout[].marks`)를 읽고,
  악보 클릭 시크는 그 역함수라 같은 지점으로 되돌아온다.
- 박자 양자화 전후를 명확히 구분해 표시한다. (남음)

완료 기준: 같은 음이 악보, 피아노 롤, 재생 위치에서 같은 시간·음높이로 확인된다.

## 3. 누락·오인식 검증 화면

- CQT 기반 누락 후보를 악보/피아노 롤에 검토 표시한다.
- 후보를 자동으로 악보에 넣지 않고 사용자가 승인할 때만 반영한다.
- 음 하나가 사라진 사례처럼, 후보의 시간·음높이·근거 에너지를 확인할 수 있게 한다.

완료 기준: 검증 후보를 듣고 보고 수정할 수 있으며, 자동 보정이 원본 결과를 훼손하지 않는다.

## 4. 성부/악기 개념 분리

- 화면 용어를 `악기 파트`, `시퀀스`, `악보 성부`로 구분한다.
- 피아노처럼 한 악기가 화음을 연주한 경우에는 여러 독립 악기로 잘못 표시하지 않는다.
- ~~장기적으로 피아노 파트의 대보표(오른손/왼손) 표기를 지원한다.~~
  `_plan_staves`가 음역대로 두 단으로 나누고, 각 단은 최대 2성부까지만 인쇄한다.
  남은 문제: 한 성부가 가온다를 넘나들면 그 성부 전체가 한쪽 단에 남는다.

완료 기준: 사용자에게 “몇 명의 연주자”를 추정한다고 오해시키지 않고, 음악적으로 읽기 좋은 악보를 제공한다.

## 5. GPU 실행 경로

- `deploy/VAST_GPU.md`의 단계적 계획대로 MT3 원격 입력/출력 프로토콜을 구현한다.
- 로컬 CPU 기본 경로는 유지하고, GPU가 필요할 때만 서버리스 엔드포인트를 사용한다.
- 짧은 테스트 음원으로 비용·대기 시간·결과 일치를 비교한다.

완료 기준: GPU 미사용 시 비용이 없고, GPU 사용 시에도 동일한 결과 형식과 검증 절차가 적용된다.
