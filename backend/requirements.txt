# Core web + audio stack (reliable, always installed)
setuptools<81            # pretty_midi still imports pkg_resources
fastapi==0.115.6
uvicorn[standard]==0.32.1
python-multipart==0.0.20
numpy<2.0
scipy==1.13.1
librosa==0.10.2.post1
soundfile==0.12.1
pretty_midi==0.2.10
yt-dlp>=2025.1.1              # YouTube URL -> audio (needs cookies.txt on server IPs)
yt-dlp-ejs>=0.8.0            # offline JS "n" challenge solver (needs Deno on PATH)
bgutil-ytdlp-pot-provider>=1.3.2  # GVS PO tokens; also run the bgutil-pot HTTP server

# Optional: high quality polyphonic transcription.
# Installed separately by setup.sh (heavy dependency, tolerated to fail).
# basic-pitch[onnx]

# Optional: trained note-detection models (reuse the torch already pulled by
# demucs). Absent -> falls back to pYIN / basic-pitch.
#   torchcrepe                      -- CNN monophonic pitch (bass/vocal/lead stems)
#   piano_transcription_inference   -- Kong et al. dedicated piano model
#   mir_eval                        -- for backend/eval_harness.py

# Optional: "stems" mode = multi-instrument separation. Heavy (~1.5 GB, torch).
# Install torch/torchaudio from the CPU wheel index first, then demucs 4.0.1
# (>=4.1 pulls a Rust build). Absent -> stems mode is hidden.
#   pip install torch==2.4.1 torchaudio==2.4.1 --index-url https://download.pytorch.org/whl/cpu
#   pip install demucs==4.0.1
