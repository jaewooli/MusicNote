# 정확도 개선 작업 지시서

`CLAUDE_CODE_HANDOFF.md`(codex 작성)를 읽고, 실제로 측정한 뒤 정리한 문서다.
핵심 결론: **codex가 만든 평가 하네스는 한 번도 실행된 적이 없고, 그동안의
정확도 판단은 Canon 클립 한 개의 육안 관찰에 근거하고 있었다.**
아래 숫자는 2026-08-30에 `eval/refs`(6클립, 3281음) 로 처음 측정한 값이다.

## 0. 측정된 기준선 (baseline)

### 0-1. 중요한 정정: 두 경로는 서로 다른 엔진이다

처음에 `direct:polyphonic` 으로 잰 값(note F1 0.526)은 **출시 경로가 아니다.**
`eval_harness.py --engine direct:` 는 `transcribe.py` 의 basic-pitch 를 부르는데,
사용자가 `polyphonic` 을 고르면 `app.py:519` 의 `is_mt3` 때문에 **MT3** 가 돈다.
아래 숫자는 실제 MT3 출력으로 다시 잰 것이다.

### 0-2. MT3 경로 (실제 출시 경로)

`python eval/replay_eval.py eval/refs` — MT3 raw 출력 캐시를 재생해서 채점.

| min_note 게이트 | note P | note R | note F1 | note+offset F1 | 게이트 삭제 |
|---|---|---|---|---|---|
| **0.080 (기존 기본값)** | 0.985 | 0.096 | **0.175** | 0.152 | **542/610** |
| 0.035 | 0.992 | 0.369 | 0.538 | 0.513 | 351/610 |
| 0.020 | 0.987 | 0.555 | 0.711 | 0.687 | 218/610 |
| **0.005 (수정 후)** | 0.926 | 0.811 | **0.865** | **0.811** | **0/610** |

(ref00 기준. 나머지 클립은 캐시 완료 후 갱신)

### 0-3. 참고: basic-pitch 경로 (출시 안 됨)

onset F1 0.717 / note F1 0.526 / note+offset F1 0.086,
precision 0.818 vs recall 0.650. 라이브러리 모드용 참고치로만 유지한다.

### 이 숫자가 말하는 것

1. **MT3 자체는 나쁘지 않았다.** 게이트를 끄면 note F1 0.865, note+offset F1
   0.811 이 나온다. 문제는 모델이 아니라 후처리였다.
2. **문제는 과검출이 아니라 미검출이었다.** precision 0.93 / recall 0.81.
   그리고 그 미검출의 대부분은 아래 P1 한 줄에서 나왔다.
3. **basic-pitch 로 관찰한 "음 길이가 2.9배 길다"는 MT3 에 해당하지 않는다.**
   MT3 는 오히려 짧게 낸다(중앙값 30 ms, 정답 45 ms). 원래 P2 로 잡았던
   offset 정제는 출시 경로에서는 우선순위가 크게 낮아진다. 8절 참조.

---

## 1. [P0] 기준선을 회귀 게이트로 고정한다 — 완료

**문제였던 것**: `backend/eval_harness.py` 와 `eval/refs` 가 갖춰져 있는데도
실행 기록이 저장소 어디에도 없었다. 최근 세 커밋은 모두 이 지표에 어떤
영향을 줬는지 확인되지 않은 채 들어갔다.

**한 일**
- `backend/mt3_post.py` (신규): `app._mt3_stems` 에 인라인으로 있던 게이트를
  순수 함수로 분리. env 로 오버라이드 가능하고, **몇 개를 왜 지웠는지
  리포트를 반환**한다. 이전에는 조용히 사라졌다.
- `eval/mt3_cache.py` (신규): 클립당 MT3 raw 출력을 한 번만 캐시한다.
  이 서버에서 MT3 는 실시간의 약 13배라 매번 추론하면 튜닝이 불가능하다.
- `eval/replay_eval.py` (신규): 캐시를 읽어 게이트 + 시퀀스 분리를 재생하고
  채점한다. `--sweep-min-note` 로 파라미터 스윕. **초 단위로 돈다.**
- `eval/test_mt3_gate.py` (신규): 게이트 회귀 테스트.
- `eval_harness.py` docstring 에 direct/api 엔진 차이 경고 추가.

**하네스 자체 검증**: 정답 MIDI 를 그대로 MT3 출력인 척 주입하면
(= 완벽한 채보) note F1 0.992 가 나온다. 채점 배관이 맞다는 뜻이다.

**남은 것**: 정확도에 영향을 주는 변경은 전후 note F1 을 같이 적는다.
올랐는지 내렸는지 모르는 변경은 병합하지 않는다.

---

## 2. [P1] 80 ms 최소 길이 필터 — 완료, 영향이 가장 컸다

**문제**: `backend/app.py:377-382` (수정 전)

```python
amp_thr = int(round(18 * (1.0 - s)))   # s=0.5 -> 9
min_len = 0.05 + 0.06 * (1.0 - s)      # s=0.5 -> 0.08 s
if n["velocity"] < amp_thr or (n["end"] - n["start"]) < min_len:
    continue
```

MT3 는 프레임 피크가 아니라 **이산 노트 이벤트**를 낸다. ref00 에서 MT3 raw
음 길이 중앙값은 30 ms, 88.9 % 가 80 ms 미만이다. 정답도 중앙값 45 ms,
86.4 % 가 80 ms 미만이다 — 즉 짧은 음이 정상이다.
그런데 이 게이트는 **610 음 중 542 음(89 %)을 지우고 있었다.**

결과: note F1 0.175. 완벽한 채보를 넣어도 이 게이트로는 0.564 가 상한이었다.

**추가 발견**: YourMT3 는 **모든 음의 velocity 를 100 으로 고정**해서 낸다.
따라서 velocity 게이트는 한 번도 발동한 적이 없고, 감도 슬라이더는 MT3
경로에서 **오직 짧은 음을 지우는 기능만** 하고 있었다.
`app.py` 의 `conf = 0.55 + 0.4*(velocity/127)` 도 전 음 동일한 상수라
`low_conf` 표시는 의미가 없다.

**한 일**
- 길이 하한을 0.005 s 고정으로 낮췄다(0 ~ 0.008 구간은 점수가 동일).
  길이는 이제 필터가 아니라 **퇴화 이벤트 방어용 검사**다.
- 감도가 길이를 건드리지 못하도록 `MIN_NOTE_SPAN = 0.0` 으로 분리했다.
- 게이트가 버린 개수를 로그로 남긴다.

**결과**: ref00 note F1 **0.175 → 0.865**, note+offset F1 0.152 → 0.811.

**남은 결정**: MT3 경로에서 감도 슬라이더는 이제 아무 효과가 없다.
MT3 가 per-note confidence 를 주지 않기 때문이다. 길이로 대체하면 안 된다
(그게 이 버그였다). 슬라이더를 MT3 모드에서 숨기거나, 별도의 신뢰도
추정을 붙이는 결정이 필요하다. **사용자 판단 필요.**

---

## 3. [P2] 음 길이(offset) 정제 — 우선순위 강등

원래 note+offset F1 0.086 을 근거로 P2 로 올렸으나, 그 값은 basic-pitch
경로의 것이었다. **MT3 경로는 게이트 수정 후 0.811 이다.**

다만 MT3 는 음을 짧게 내는 경향이 있다(20 ms 미만이 35.7 %, 정답은 1.9 %).
mir_eval 의 offset 허용치가 짧은 음에서는 최소 50 ms 로 동작하므로 지표에
잘 안 잡힐 뿐, 악보 표기에서는 문제가 될 수 있다.

**할 일 (낮은 우선순위)**
- 악보용 최소 표기 길이는 채점용 offset 과 분리해서 다룬다.
- `voices.py:91` 의 0.18 s 초과 허용은 연결 판단에만 쓰고 기록되는 `end`
  에는 반영하지 않도록 분리한다.

---

## 4. [P3] 시간 이동 앙상블로 누락을 줄인다

**문제**: `backend/mt3_worker.py` 는 파형 전체를 한 번만 추론하고,
codex는 끝부분 누락에 대해 오른쪽 0.75 s 패딩만 추가했다
(`END_PADDING_SECONDS`). 이건 **클립 끝** 문제만 다룬다. Canon의
8.208 s E4 누락처럼 **곡 중간** 누락은 모델 내부 세그먼트 경계에서
생기는데, 패딩으로는 손댈 수 없다.

**할 일**
- 같은 오디오를 시간 오프셋을 달리해(예: 0 s, 세그먼트 절반) 2~3회 추론하고
  결과를 합친다. 한 번이라도 잡힌 음은 후보로 살리고, 중복은
  onset ±50 ms / 동일 피치로 병합한다.
- 병합 규칙을 union(재현율 우선)과 vote(정밀도 우선) 두 가지로 만들고
  하네스로 어느 쪽이 note F1 이 높은지 고른다.
- 비용은 추론 횟수에 비례하므로(현 CPU 기준 곡 길이의 8배 × N),
  이 항목은 P4(GPU 경로)와 함께 판단한다.

**완료 기준**: Canon 클립 8.208 s E4 가 후보가 아니라 실제 출력에 잡히고,
`eval/refs` recall 이 하락 없이 상승.

---

## 5. [P4] Demucs 와 MT3 가 서로 배타적이다

**문제**: `backend/app.py:524` — `use_demucs = mode == "stems"`.
MT3 경로는 원본 믹스를 그대로 받는다. 혼합 음원에서 스템 분리 후 채보는
표준적인 정확도 향상 수단인데, 지금은 둘 중 하나만 쓴다.
codex 문서에도 이 조합에 대한 언급이 없다.

**할 일**
- 실험 플래그로 `demucs → 스템별 MT3 → 병합` 경로를 만든다.
- 하네스로 `api:mt3` vs `api:mt3+demucs` note F1 을 비교한다.
- 메모리 슬롯(`_STEMS_SLOT`/`_MT3_SLOT`)이 이미 직렬화돼 있으므로
  순차 실행은 가능하다. 대신 소요 시간이 크게 늘어난다.

**완료 기준**: 밴드/보컬 포함 클립에서 note F1 향상이 확인되면
해당 음원 유형에 한해 기본 경로로 승격.

---

## 6. [P5] 누락 검증기(quality.py) 자체가 검증돼 있지 않다

**문제**: `missing_onset_candidates` 의 임계값(레벨 0.58, 상승 0.35,
간격 0.08 s, 길이 0.09 s)은 **Canon 클립 한 개**에서 나온 상수다.
`confirm_candidates_with_second_pass` 도 마찬가지로 근거가 없다.
이 검증기가 실제 누락을 몇 % 찾아내는지, 헛다리를 몇 개 짚는지 아무도 모른다.

**핵심**: `eval/refs` 에는 정답 MIDI가 있으므로 **이걸 정확히 측정할 수 있다.**

**할 일**
- 스크립트를 하나 만든다: 정답 대비 실제로 놓친 음 집합을 구하고,
  `confirmed_missing_notes` 가 그중 몇 개를 맞혔는지(recall),
  놓치지 않은 음을 몇 개 잘못 지목했는지(precision)를 출력한다.
- 그 숫자를 보고 임계값을 조정한다. 지금처럼 한 곡 눈대중으로 만지지 않는다.
- 검증기 precision 이 낮으면 UI에 그리는 순간 사용자 신뢰를 잃는다.
  UI 작업(P2 of TODO.md)보다 이 측정이 먼저다.

**완료 기준**: 검증기의 precision/recall 수치가 문서에 있고,
임계값 변경이 그 수치로 정당화된다.

---

## 7. [P6] 시퀀스 분리는 합성 테스트 5개뿐이다

**문제 1**: `eval/test_sequence_analysis.py` 는 손으로 만든 5개 케이스만
검사한다. 실제 오디오 회귀가 없다 (TODO.md 1번이 아직 안 됨).

**문제 2**: `voices.py:separate_sequences` 의 병합이 union-find 무조건
결합이다.

```python
for _score, i, j in sorted(proposals):
    join(i, j)
```

A~B 가 화음이고 B~C 가 화음이면 A~C 를 검사하지 않고 A,B,C 가 한 시퀀스가
된다. 체인이 길어지면 독립 성부가 통째로 하나로 뭉칠 수 있다.

**할 일**
- 병합 후보를 결합할 때 이미 합쳐진 그룹 전체와 다시 점수를 계산하거나,
  최소한 결합 결과의 음역 폭 / 간격 분산 상한을 둔다.
- 실제 클립(피아노 독주, 반주+멜로디, 밴드)으로 회귀 세트를 만든다.
- 시퀀스 분리는 note F1 을 바꾸지 않으므로(음 집합은 보존) 별도 지표가
  필요하다: 사람이 매긴 정답 성부와의 일치율.

**완료 기준**: 체인 병합 반례가 테스트에 있고, 실제 클립 회귀가 돈다.

---

## 8. [P7] 레퍼런스 세트가 제품과 다르다

**문제**: `eval/refs` 6클립은 전부 MAESTRO 피아노 독주를 fluidsynth 로
렌더한 것이다. `eval/build_refs.py` 주석도 "실제 녹음보다 깨끗해서 F1이
낙관적으로 나온다"고 스스로 밝히고 있다. 그런데 제품의 주 사용처는
YouTube Shorts / 밴드 / 보컬이다. 즉 **지금 숫자는 상대 비교용으로만 유효하다.**

**할 일**
- 실제 녹음 기반 세트를 추가한다 (MAPS/MAESTRO 실연 녹음, Slakh 등).
- 최소한 피아노 독주 / 보컬+반주 / 밴드 3개 카테고리로 나눠 평균을 낸다.
- 카테고리별로 note F1 을 따로 본다. 하나의 평균은 회귀를 감춘다.

**완료 기준**: 소프트폰트 세트와 실녹음 세트의 note F1 차이를 알고 있고,
튜닝이 실녹음 쪽을 기준으로 이뤄진다.

---

## 작업 순서 요약

| 순 | 항목 | 상태 | 효과 |
|----|------|------|------|
| 1 | 기준선 고정 (P0) | **완료** | 이후 모든 판단의 전제 |
| 2 | 80 ms 게이트 (P1) | **완료** | note F1 0.175 → 0.865 |
| 3 | 감도 슬라이더 처리 | **판단 필요** | MT3 경로에서 무효 |
| 4 | 검증기 측정 (P5) | 다음 | UI 작업의 전제 |
| 5 | 시간 이동 앙상블 (P3) | 대기 | 남은 recall 0.81 → ? |
| 6 | 시퀀스 회귀 (P6) | 대기 | 정확도 아닌 가독성 |
| 7 | Demucs+MT3 (P4) | 대기 | 혼합 음원 |
| 8 | 실녹음 세트 (P7) | 대기 | 측정 신뢰도 |
| 9 | offset 정제 (P2) | 강등 | MT3 는 이미 0.811 |

## 재현 명령

```bash
# MT3 raw 출력 캐시 (클립당 약 5분, 최초 1회만)
python eval/mt3_cache.py eval/refs

# 후처리 채점 — 초 단위
python eval/replay_eval.py eval/refs
python eval/replay_eval.py eval/refs --sweep-min-note 0.005,0.02,0.08

# 회귀 테스트
python eval/test_mt3_gate.py
python eval/test_sequence_analysis.py

# 전체 파이프라인 (느림: MT3 는 실시간의 약 13배)
python backend/eval_harness.py --engine api:mt3 eval/refs
```
